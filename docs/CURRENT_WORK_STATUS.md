# WisdomAI — สถานะงานและรายการตรวจรับ

### CHAT-ATTACHMENT-001 — Web Chat แนบไฟล์และกู้คืน session (23/8/2569)

- สถานะ: `ขึ้น Production แล้ว รอ authenticated UAT` | Vercel deployment `dpl_2c1TjsMGotYsh5JSTYZsuhdJ52tW` `READY`, alias `https://wisdomai-react.vercel.app`
- ขอบเขต: หน้า `/chat` ตรวจ MIME/ขนาดตั้งแต่เลือก, แสดง pending attachment card พร้อมชื่อ/ขนาด, ใช้ปุ่ม `ส่งไฟล์`, ตรวจ `session.expires_at` และ refresh ก่อน upload, normalize body MIME, แยก session หมดอายุออกจากสิทธิ์ห้อง และคงไฟล์ไว้ให้ retry หลัง login ใหม่หรือเมื่อ Storage/network/insert ล้มเหลว; ไม่เพิ่ม policy/schema ใหม่ในรอบนี้
- หลักฐาน: `npm run test:chat-launcher-attachment`, targeted ESLint, `npm run lint`, `npm run build`, `git diff --check` ผ่าน; Supabase policy simulation สำหรับบัญชี `3836862f-7062-4b27-a1e9-6d8032266c8c` ผ่านทั้ง company/room/manager; Vercel inspect เป็น `READY`, error log deployment ไม่พบรายการ
- Root cause/UAT: Production log ก่อนแก้พบ `auth refresh 400` และ Storage `row-level security` เวลา 19:47 UTC ขณะบัญชียังเป็นสมาชิก/manager; จึงแก้ข้อความและ preflight session แล้ว แต่ environment นี้ไม่มี authenticated browser session สำหรับยืนยัน upload object + `chat_messages` จริง; หลังเหตุการณ์ยังพบ `chat-attachments` object 0 และ file message 0 ไม่มี legacy file record ให้ migrate
- Rollback: Vercel `rollback` ไป deployment ก่อนหน้าได้; การเปลี่ยนนี้ไม่ลบห้อง ข้อความ หรือไฟล์

### CHAT-ATTACHMENT-002 — ซิงก์ Web Chat attachment ไปยัง fallback host (23/8/2569)

- สถานะ: `Vercel และ Cloudflare ใช้ release เดียวกันแล้ว; รอ authenticated UAT upload จริง`
- Root cause ที่ตรวจพบ: Auth/หน้าใช้งานจริงของผู้ใช้มาจาก `https://wisdomai.pages.dev` แต่ Cloudflare ยังเสิร์ฟ bundle รุ่น `bcabf07` ที่เริ่ม upload ทันทีและไม่มี session recovery; Supabase log จึงไม่เห็น upload ใหม่หลังการเข้าสู่ระบบรอบล่าสุด และยังมีหลักฐานเดิมของ `auth refresh 400` + Storage RLS
- ขอบเขต: Vercel และ Cloudflare ใช้ artifact commit `a722ea3` ที่มี pending attachment, ปุ่ม `ส่งไฟล์`, session refresh/retry, MIME normalization และ error mapping; ไม่เปลี่ยน schema, bucket, policy หรือข้อมูลเดิม
- Verification gate: `npm run test:chat-launcher-attachment`, targeted/full lint, build, Vercel deployment `dpl_GQfXKkqeun7kXqSL2RSgpMQTEd4q` READY/ไม่มี error log และ Cloudflare bundle parity ผ่าน; เหลือ authenticated UAT ยืนยัน `เลือกไฟล์ → pending → ส่งไฟล์ → storage.objects → chat_messages → signed URL`
- Rollback: rollback artifact ของทั้งสอง host ไป release ก่อนหน้า; object/message ที่สำเร็จแล้วไม่ถูกลบ

### CHAT-ATTACHMENT-003 — ลากไฟล์วางใน Web Chat (23/8/2569)

- สถานะ: `ขึ้น Vercel/Cloudflare แล้ว; รอ authenticated drag-drop UAT`
- ขอบเขต: เพิ่ม drag-and-drop zone ในพื้นที่ห้องแชต; ไฟล์ที่วางจะเข้าสู่ validation และ pending/send flow เดิม ไม่ upload ทันที
- Verification: `npm run test:chat-launcher-attachment`, targeted/full ESLint และ `npm run build` ผ่าน; Vercel GitHub checks ของ commit `ce1a638` สำเร็จ; Cloudflare Chat bundle `Chat-Bdwx3XB9.js` พบ `วางไฟล์ที่นี่`, `ลากไฟล์มาวางในพื้นที่แชต`, `dropEffect` และ `refreshSession`; เหลือยืนยันหน้า Production ด้วยการลากไฟล์จริงและตรวจ `storage.objects` + `chat_messages`
- Rollback: ถอด handlers/overlay ได้โดยไม่ลบไฟล์หรือข้อความที่ส่งสำเร็จ

### PROD-20260823-01 — Production readiness deployment (23/8/2569)

- สถานะ: `พร้อมใช้งาน` | Deploy ล่าสุด `dpl_DhfpusL8kHA6to6mMgYJCC8Xu2GZ` ขึ้น Vercel Production สำเร็จและ alias เป็น `https://wisdomai-react.vercel.app`; Supabase `line-webhook` version 63 เป็น `ACTIVE`
- ขอบเขต: รวมโค้ดปัจจุบันทั้งหมด โดยเฉพาะ Application Launcher/การแยกทางเข้า mobile–desktop ตาม role, Web Chat/ลงเวลา, attachment intake, Flow Registry และการปิดคำ generic LINE “ลงเวลา”; ไม่มีการเปลี่ยน Migration หรือข้อมูล Production ในรอบนี้
- หลักฐาน: `npm run lint`, `npm run build`, `test:auth-routing`, `test:smart-entry`, `test:chat-launcher-attachment`, `test:cheque-payment-intake`, `test:system-error-intake` และ `test:line-attendance-command` ผ่าน; local preview ที่พอร์ต 4173 เปิด `/`, `/login`, `/dashboard`, `/my-profile`, `/time-tracking`, `/chat` ได้โดย route ที่ต้อง login redirect ไป `/login`; manifest และไอคอน 192/512 ตอบ HTTP 200; Vercel inspect เป็น `READY`, ไม่พบ error logs ใน 1 ชั่วโมงล่าสุด, Supabase function list ยืนยัน version 63 ACTIVE และ Production query ไม่พบ generic attendance requests
- ผลกระทบ/การกู้คืน: ผู้ใช้ได้รับ artifact ล่าสุดทั้งสอง host โดยไม่เปลี่ยนสิทธิ์หรือ schema; หากต้องย้อนกลับให้ใช้ Vercel rollback/Promote และ rollback Cloudflare release เดียวกัน

### LINE-ATTENDANCE-COMMAND-001 — ปิดการตีความคำกำกวม “ลงเวลา” (23/8/2569)

- สถานะ: `ขึ้น Production แล้ว` | `line-webhook` version 63 เป็น `ACTIVE`; LINE จะไม่สร้างคำขอลงเวลาจาก `ลงเวลา`, `ลงเวลาทำงาน`, `บันทึกเวลา` หรือ `บันทึกเวลาทำงาน`; ต้องใช้คำสั่งที่ระบุทิศทาง เช่น `ลงเวลาเข้า`/`ลงเวลาออก`
- ขอบเขต: แยก parser เป็น `supabase/functions/line-webhook/attendance-command.ts`; คำขอทิศทางชัดเจน, สิทธิ์, GPS/Selfie/การยืนยัน และ HR Chat bridge เดิมไม่เปลี่ยน
- Migration/ข้อมูล: ไม่มี Migration; query Production ไม่พบ `line_attendance_requests` หรือ `line_task_commands` ที่มาจากคำ generic จึงไม่มีรายการค้างให้ยกเลิกหรือลบ
- Verification/rollback: parser regression test, `npm run lint`, `npm run build`, Supabase Edge Function deploy และ Production query ผ่าน; rollback โดยคืน parser และ deploy function รุ่นก่อนหน้า โดยไม่แก้ข้อมูล attendance เดิม

### PAYROLL-FORECAST-001 — ซ่อมค่าแรง Real-time และประมาณการสิ้นเดือน (11/8/2569)

- สถานะ: `เสร็จแล้ว` 100%; ผู้ใช้อนุมัติปิดงานหลังตรวจหน้า Production และ PDF รายบุคคล โดย Migration `202608120001_complete_payroll_forecast.sql` อัปเดตแถว `PAYROLL-FORECAST-001` เดิมเป็น done และไม่สร้างงานซ้ำ
- หลักฐาน: Migration `202608110036_fix_realtime_payroll_forecast_rpc.sql` ขึ้น Supabase Production สำเร็จ; `test:payroll-forecast`, lint และ build ผ่าน (lint มี warning เดิม 1 จุด ไม่มี error); Vercel deployment `DCGXyEtuEGtKL2QimDwQwkFbAxyN` alias `https://wisdomai-react.vercel.app`; `/reports` ตอบ HTTP 200 และพบ app root
- Production status: `deployed_pending_signed_in_payroll_forecast_uat`; หน้าเว็บแยกข้อความไม่มีสิทธิ์ / RPC ยังไม่ติดตั้ง / RPC คำนวณล้มเหลว เพื่อไม่รายงานสาเหตุผิดอีก
- อัปเดตหน้า Reports 12/8/2569: สรุปด้านบนตารางแสดงพนักงาน, รายการรอตรวจ, วันสุทธิ, เวลาสุทธิ, สาย (วัน/นาที), ออกก่อน (วัน/นาที), ออกเกิน (วัน/เวลา), OT อนุมัติ และค่าแรง/เงินเดือนประมาณการ; ตารางรวมพนักงานรายเดือนที่ไม่บังคับลงเวลา แยกวันมาทำงานกับวันสุทธิ และระบุยอดจาก Payroll หรือประมาณการ. `npm.cmd run lint` ผ่านโดยมี warning เดิม 1 จุด, `npm.cmd run build` และ `npm.cmd run test:payroll-forecast` ผ่าน; Vercel Production deployment `75jaFBB4Ha8JsvHtsRhhrDjUus3A` alias `https://wisdomai-react.vercel.app`; `/reports` HTTP 200 และ bundle พบข้อความใหม่ครบ. คง `review` จน Signed-in UAT ตรวจตัวเลขจริง.
- ปิดงาน 12/8/2569: ตรวจ Migration local/remote พบรายการค้างเฉพาะ `202608120001`; Production db push นำขึ้นเฉพาะรายการนี้สำเร็จและตรวจซ้ำพบ local/remote ตรงกัน. สถานะฐานข้อมูลหลักเป็น done 100% / `deployed_uat_approved`.

### TEN-001 — Attendance Edge Tenant UAT (10/8/2569)

- สถานะ: `เสร็จแล้ว` 100%; ผู้ใช้อนุมัติให้ปิดก่อนและเลื่อน authenticated two-company UAT ไปตรวจภายหลัง; Worker Run ปิดงาน `153f46df-b114-4d3d-99c3-c05ab0e4640d` สำเร็จและไม่มี active run ค้าง
- ขอบเขต: ตรวจ active company/membership, employment/site/assignment/session/settings/notifications และ negative cross-tenant guard ของ `attendance-clock`
- ความเสี่ยง: วิกฤต | หลักฐาน: `test:attendance-tenant`, lint และ build ผ่าน; anonymous Production smoke ตอบ 401 ตามคาด; Production queue ยืนยัน `done` 100% | Production status: `deployed_uat_deferred_by_user`

### TEN-009 — Storage Tenant Security (10/8/2569)

- สถานะ: `เสร็จแล้ว` 100%; ผู้ใช้อนุมัติปิดและเลื่อน Storage UAT สองบริษัทไปตรวจภายหลัง; Migration `202608100010` ขึ้น Production และซิงก์แถวเดิมสำเร็จโดยไม่มี active run ค้าง
- ขอบเขต: บังคับ company namespace และ tenant-aware policy สำหรับ `line-attachments`, `drawing-ai`, `boq-imports` พร้อมรองรับ path เก่าแบบจำกัดตาม metadata ของบริษัท
- ความเสี่ยง: วิกฤต/ความปลอดภัย | หลักฐาน: Migration `202608100009` และ `202608100010` ขึ้น Production; `test:storage-tenant`, lint และ build ผ่าน; Vercel Production READY; BOQ HTTP 200; line-webhook ปฏิเสธ signature ผิดด้วย HTTP 401; Production queue ยืนยัน `done` 100% | Production status: `deployed_storage_uat_deferred_by_user`

### SYS-001 — ปรับ Tab และตัวกรองหน้าสถานะระบบ (10/8/2569)

- สถานะ: `เสร็จแล้ว` 100% | ขอบเขต: เปลี่ยน Tab ให้สื่อขั้นตอนงาน (ต้องดำเนินการ/กำลังทำ/รออนุมัติ/รอตรวจ/ติดปัญหา/เสร็จแล้ว), แยกประเภทงานเป็นตัวกรอง และไม่แสดงปุ่มอนุมัติกับงาน UAT ที่เป็นเพียงรอตรวจ
- ความเสี่ยง: ต่ำ | หลักฐานทดสอบ: lint, build และ `test:health-status-report` ผ่าน; Vercel Production deployment `2U79NJwvjc3ChLtWLf2PKsc49efX`; `/system-health` ตอบ HTTP 200 และมี app root | Production status: `deployed_smoke_passed`
- หมายเหตุ: ยังซิงก์หลักฐานรอบนี้กลับฐานข้อมูลหลักไม่ได้เพราะ Automation Worker credential ตอบ 401; บันทึกในเอกสารสำรองแล้วและต้องซิงก์กลับแถว SYS-001 เดิมเมื่อ credential ใช้งานได้
- เพิ่มเติม: หน้า Status แยก 5 Tab (ภาพรวม/งานระบบ/ปัญหาและเหตุการณ์/Log และการสื่อสาร/ตั้งค่าและประวัติ), กรอบผลตรวจรองรับคีย์บอร์ดและคลิกเปิด Drawer, แสดง metadata/เวลา/latency และลิงก์รายงานลงเวลา; lint/build/test ผ่าน; Deploy `9uoPcrnadavjgXQ3HmbYbEzCepuQ`; Production `/system-health` HTTP 200

### SYS-004 — แก้ Attendance Health Warning ค้างจากประวัติเก่า (10/8/2569)

- สถานะ: `รอตรวจ` 95% | ขอบเขต: นับการแจ้งเตือนล้มเหลวเฉพาะ 24 ชั่วโมงล่าสุด, ไม่นับรายการที่ retry แล้วเปลี่ยนสถานะสำเร็จ/เข้าคิว, แยกจำนวนตามบริษัทใน metadata และคงการตรวจ session ไม่มีเวลาออกเกิน 18 ชั่วโมง
- ความเสี่ยง: กลาง | หลักฐานทดสอบ: `test:health-attendance-warning`, `test:health-status-report`, lint และ build ผ่าน; Deploy `health-monitor` Production สำเร็จ; anonymous smoke ตอบ HTTP 401 ตามคาด | Production status: `deployed_waiting_scheduled_result`
- Fingerprint: `health.attendance.historical_failed_notification_false_warning` | หมายเหตุ: บันทึกในเอกสารสำรองก่อน เนื่องจาก Worker credential ยังตอบ 401

### SYS-010 — ตรวจคืนการเชื่อมต่อ Automation Worker (10/8/2569)

- สถานะ: `เสร็จแล้ว` 100% | สาเหตุ 401 เดิมคือคำสั่งตรวจใช้ Header `x-worker-secret` ไม่ตรงกับสัญญา Function ที่กำหนด `x-automation-worker-secret`; Secret เดิมไม่ได้เสียและไม่ต้องหมุน
- ความเสี่ยง: ต่ำ | หลักฐาน: authenticated Production status ตอบสำเร็จด้วย credential เดิม; คิว 25 งาน = done 19 / ready 6; active run 0; ไม่มีการเปิดเผยหรือเปลี่ยน Secret | Production status: `worker_authenticated_no_rotation_required`

### LOG-001 — ศูนย์รวม Log และการสื่อสาร (10/8/2569)

- สถานะ: `เสร็จแล้ว` 100% | ขอบเขต: รวม LINE, Telegram, Health notification, Attendance approval และ Work-item audit เป็น feed เดียวแบบไม่คัดลอกข้อมูลซ้ำ; แสดงช่องทาง ข้อความ สถานะ ผู้อนุมัติและเวลา; แยกบริษัท/RLS และ Export ตามสิทธิ์
- ความเสี่ยง: สูง (Migration/RLS/Tenant) | หลักฐานทดสอบ: `test:communication-event-feed`, `test:health-attendance-warning`, lint และ build ผ่าน; Migration `202608100012` local/Production ตรงกัน; Deploy `health-monitor` สำเร็จ; anonymous smoke ถูกปฏิเสธ 401; `/system-health` ตอบ HTTP 200 | Production status: `deployed_and_smoke_passed`

### HLT-001 — รายละเอียดและการจัดการปัญหาลงเวลาจาก Health Monitor (10/8/2569)

- สถานะ: `เสร็จแล้ว` 100% | ขอบเขต: แสดงชื่อพนักงาน รหัสพนักงาน บริษัท โครงการ ไซต์ เวลาเข้าและระยะเวลาค้างแทน UUID; เชื่อมตรงไปยังรายการลงเวลาเป้าหมาย; เปิด Tab รอตรวจและฟอร์มแก้ไขโดยใช้ `admin_save_attendance` ซึ่งตรวจสิทธิ์และบันทึก audit เดิม
- ความเสี่ยง: กลาง (ข้อมูลบุคคล/สิทธิ์/การแก้เวลาที่กระทบค่าจ้าง) | หลักฐานทดสอบ: `test:health-attendance-warning`, lint และ build ผ่าน; Deploy `health-monitor` และ Vercel Production `BC8rYLpUc3r6PXLpaornpBwTHpoq`; `/system-health` และ Reports deep link ตอบ HTTP 200; anonymous Function ตอบ 401 | Production status: `deployed_smoke_passed_pending_admin_visual_uat`

### HLT-002 — Soft Delete เคสลงเวลาสำหรับ Platform Admin (10/8/2569)

- สถานะ: `เสร็จแล้ว` 100% | ขอบเขต: ให้เฉพาะ Platform Admin ยกเลิกเคสลงเวลาที่ผิดได้แบบไม่ลบหลักฐานจริง; บังคับเหตุผลและยืนยันสองชั้น; ตัดออกจาก Health/Payroll/รายการใช้งาน; เก็บ Audit และรองรับการกู้คืน
- ความเสี่ยง: สูง (สิทธิ์/ข้อมูลบุคคล/Payroll/Migration) | หลักฐานทดสอบ: `test:attendance-soft-delete`, lint และ build ผ่าน; ตรวจ Platform Admin ซ้ำใน RPC, จำกัด active company, ปิดกั้น Payroll ที่ปิดแล้ว และป้องกันการกู้คืนซ้ำวัน; Migration `202608100013` ตรงกัน Local/Remote; Vercel Production deployment `4FsRn22eRfkYfWJcMD2iZZUUehws` | Production status: `deployed_smoke_passed_pending_platform_admin_uat`

### ATT-VALIDATE-001 — ตรวจเวลาเข้า–ออกค้างและข้ามวัน (10/8/2569)

- สถานะ: `เสร็จแล้ว` 100% | ขอบเขต: ตรวจรายการเข้าเดิมก่อนลงเข้าใหม่; แยกรายการค้างข้ามวันไปรอตรวจโดยไม่สะสมเวลา; ตรวจระยะเวลากะสูงสุดและกะข้ามคืนทั้ง Web/LINE; ไม่ส่งข้อมูลผิดปกติเข้าค่าจ้างจนอนุมัติ
- ความเสี่ยง: สูง (Attendance/Payroll/Migration/Edge Functions) | หลักฐานทดสอบ: Migration `202608100014` ตรงกัน Local/Remote; Deploy `attendance-clock` และ `line-webhook` สำเร็จ; anonymous attendance ตอบ 401 และ LINE signature ไม่ถูกต้องตอบ 401; Database guard กัน open/cross-day/excessive duration ออกจากยอดจ่าย; `test:attendance-session-validation`, lint และ build ผ่าน; ผู้ใช้อนุมัติปิด 100% และเลื่อน Attendance UAT ไปตรวจภายหลัง | Production status: `deployed_uat_deferred_by_user`

### ATT-REPAIR-001 — เสนอซ่อมข้อมูลลงเวลาเดิมที่ผิดปกติ (10/8/2569)

- สถานะ: `เสร็จแล้ว` 100% | ขอบเขต: แยก Tab ซ่อมย้อนหลังออกจากรอตรวจ; ปิดข้อเสนอค้างหลัง Admin แก้เวลา/สแกนซ้ำ; เพิ่มการยืนยันว่าเวลาถูกต้องโดยไม่เปลี่ยนเวลา พร้อมผู้อนุมัติ เหตุผล Payroll lock และ Audit
- ความเสี่ยง: สูง (Attendance approval override/Payroll/Migration) | หลักฐานทดสอบ: Migration `202608100016` ตรงกัน Local/Remote; Vercel deployment `9tGVhz1aLif5b2mqvk5U4eFHFnh2`; `/reports` ตอบ HTTP 200; `test:attendance-repair`, lint และ build ผ่าน; ผู้ใช้ยืนยันว่าแก้ไขข้อมูลหมดแล้วและอนุมัติปิด UAT เป็น done 100% | Production status: `deployed_uat_passed`

### EMP-POLICY-001 — นโยบายลงเวลาตามรูปแบบค่าจ้าง (10/8/2569)

- สถานะ: `รอตรวจ` 90% | ขอบเขต: รวมศูนย์นโยบายพนักงาน เวลา ค่าจ้าง และต้นทุนโครงการโดยใช้รายการเดิม: แยกประเภทการจ้าง/รูปแบบค่าจ้างและนโยบายลงเวลา `required / record_only / exempt`; พนักงานเงินเดือนแบบ exempt ไม่สร้าง Error/แจ้งเตือนและไม่หักเงินเดือนจากเวลา; แยกเวลาเข้าจริง เวลาเริ่มกะ มาก่อนเวลา เวลาปกติ OT ก่อน/หลังกะ เวลาที่ไม่นับและเวลาพัก; หน้า Reports แสดงชั่วโมงสะสมเป็นข้อความอ่านง่ายพร้อมค่าเฉลี่ย/สถานะผิดปกติ และให้ตัวเลขสรุปเชื่อมลงรายละเอียดรายวัน หลักฐาน รายรับงวดปัจจุบัน และต้นทุนโครงการ; ปฏิทินจำแนกวันหยุด/ไม่ได้จัดกะ/ลา/ไม่พบเวลา/ข้อมูลไม่ครบ/ไม่ต้องลงเวลา/ขาดงานที่ยืนยันแล้ว; รองรับต้นทุนประมาณการเทียบยอดรับรองและการกระจายเงินเดือนตามโครงการ/ไซต์
- ความเสี่ยง: สูง (Employee/Attendance/Payroll/Project Cost/Migration) | หลักฐานทดสอบ: Migration `202608100018` ขึ้น Supabase Production สำเร็จ; เพิ่ม attendance policy และฟิลด์ early/pre-shift/post-shift/excluded โดยใช้ schedule/approved OT เดิม; หน้า Employee/Reports รองรับนโยบาย รูปแบบเวลาอ่านง่าย ค่าเฉลี่ย drill-down และเวลาอธิบายนอกกะ; `test:employee-attendance-policy`, lint และ build ผ่าน; Vercel Deploy ถูกหยุดเพราะ working tree มีการเปลี่ยนแปลงนอกขอบเขตจำนวนมากและคำอนุมัติรอบนี้ยังไม่ครอบคลุมการ Deploy ทั้ง working tree | Production status: `schema_deployed_frontend_awaiting_full_worktree_deploy_approval`

### ATT-REVIEW-UX-001 — หน้าตรวจ Attendance แสดงสาเหตุและหลักฐาน (10/8/2569)

- สถานะ: `เสร็จแล้ว` 100% | ขอบเขต: แสดงสาเหตุภาษาไทย แหล่งที่มา GPS/Selfie/อุปกรณ์/ตารางงาน/เวลาที่ผิดปกติ; เปิดรายละเอียดจากทั้งแถว; รองรับอนุมัติ แก้เวลา ขอข้อมูลเพิ่ม และ Soft Delete ตามสิทธิ์
- ความเสี่ยง: กลาง (ข้อมูลบุคคล/การตัดสินใจ Attendance) | หลักฐานทดสอบ: เพิ่มรายละเอียดรายการรอตรวจจากทั้งแถว พร้อมพนักงาน/ไซต์/กะ/GPS/ระยะ/แผนที่/อุปกรณ์/Selfie และ action ตามสิทธิ์; `test:attendance-repair`, `test:attendance-soft-delete`, lint และ build ผ่าน; Vercel Production `DQtRmFn7LvfsHmVyQxNhck5Jsuoq`; `/reports` HTTP 200 และ production bundle พบรายละเอียด/GPS/Google Maps; Migration `202608110001` ซิงก์คิว Production | Production status: `deployed_smoke_passed`

### ATT-CHANNEL-001 — ลงเวลาผ่าน LINE/Telegram/Web Chat (10/8/2569)

- สถานะ: `เสร็จแล้ว` 100% | ขอบเขต: รับข้อความ/Voice/Location/Selfie; ระบุตัวพนักงานและบริษัท; ทวนคำสั่ง; รับรายการข้อมูลไม่ครบเป็นรอตรวจ; ใช้ Attendance รายการเดียวกันทุกช่องทาง
- ความเสี่ยง: สูง (Identity/Attendance/Multi-channel/Migration) | หลักฐานทดสอบ: Migration `202608110002` เพิ่มทะเบียนคำขอ/เหตุการณ์รวมแบบ tenant-safe, Web sync หลังสร้าง Attendance, LINE sync จาก workflow เดิม และ Telegram `/clockin` `/clockout` รับไว้แบบ information_required โดยไม่สร้าง Attendance จริง; `test:attendance-channel`, lint และ build ผ่าน; attendance-clock/telegram-admin Deploy Production; anonymous smoke 401 ทั้งคู่; Telegram `/clockin` UAT ได้เลขคำขอและแจ้งรอ Location/Selfie พร้อมยืนยันว่าไม่สร้างเวลาจริง | Production status: `deployed_uat_passed`

### ATT-IDENTITY-001 — ผูก LINE/Telegram กับพนักงานและบริษัท (10/8/2569)

- สถานะ: `เสร็จแล้ว` 100% | ขอบเขต: Mapping บัญชีช่องทางกับพนักงานแบบแยกบริษัท; ตรวจสถานะการผูก; ปฏิเสธการสร้าง Attendance จริงเมื่อระบุตัวตนไม่ได้ | หลักฐาน: Migration `202608110005` ติดตั้ง Production; identity/channel tests, lint และ build ผ่าน; telegram-admin Deploy แล้ว; anonymous smoke 401; Telegram `/clockin` UAT สร้างคำขอ `40884ae0-3c59-4eb8-ba16-ae819d1a033c` และรอ Location/Selfie โดยไม่สร้างเวลาจริง | Production status: `deployed_uat_passed`
- ความเสี่ยง: สูง (Identity/Tenant isolation) | หลักฐานทดสอบ: ผู้ใช้อนุมัติ Migration และ Production หลัง ATT-CHANNEL-001 ผ่าน UAT | Production status: `approved_for_execution`

### ATT-FALLBACK-001 — เปลี่ยนเส้นทาง LINE เต็มไป Web/Telegram (10/8/2569)

- สถานะ: `เสร็จแล้ว` 100% | ขอบเขต: Telegram รับ `/clockin`/`/clockout` ตามด้วย GPS และ Selfieภายใน 10 นาที; จับคู่พนักงาน/บริษัท/ไซต์; เก็บรูปแบบ private tenant-safe; บันทึกเวลาหรือส่งรอตรวจแบบ atomic
- ความเสี่ยง: กลาง (Attendance/Storage/Deduplication) | หลักฐานทดสอบ: Migration `202608110008`, fallback regression, Deno check, lint และ build ผ่าน; telegram-admin Deploy Production; anonymous smoke 401; Telegram Group Reply UAT คำขอ `22a2a91d-cec8-495a-acf7-e29a41e22b9f` รับ GPS/Selfie, คำนวณระยะ 143,995 เมตร และส่งรอตรวจเพราะอยู่นอกไซต์ถูกต้อง | Production status: `deployed_uat_passed`

### ATT-TELEGRAM-APPROVAL-001 — อนุมัติรายการลงเวลาผ่าน Telegram (11/8/2569)

- สถานะ: `เสร็จแล้ว` 100% | ขอบเขต: ส่งรายละเอียดรายการผิดเงื่อนไขเข้ากลุ่ม Admin พร้อมปุ่มอนุมัติ/ขอข้อมูลเพิ่ม/ไม่อนุมัติ; ตรวจ Admin และ company จากฐานข้อมูล; กันกดซ้ำ; บันทึก audit
- ความเสี่ยง: สูง (Attendance decision/Permission) | หลักฐานทดสอบ: Migration `202608110012`, approval/fallback regression, Deno check, lint และ build ผ่าน; telegram-admin และ `/approvals` Deploy Production; anonymous smoke 401; Telegram UAT กดไม่อนุมัติ Session `863c9746-c0a8-4dfb-8097-ec807b0fe9c5` และ Bot ยืนยันสถานะ `rejected` | Production status: `deployed_uat_passed`

### ATT-VOICE-001 — ถอดเสียงและทวนคำสั่งลงเวลา (10/8/2569)

- สถานะ: `พร้อมทำ` 0% | ขอบเขต: รับ Voice จาก LINE/Telegram/Web; ถอดเสียง; แสดงข้อความให้เจ้าตัวยืนยันก่อนสร้างคำขอ; เก็บไฟล์และ transcript ตามสิทธิ์
- ความเสี่ยง: กลาง (AI transcription/ข้อมูลส่วนบุคคล) | หลักฐานทดสอบ: รอ ATT-CHANNEL-001 | Production status: `not_deployed`

### CHAT-001 — Wisdom Chat ข้อความภายในและห้องสนทนา (10/8/2569)

- สถานะ: `พร้อมทำ` 0% | ขอบเขต: ห้องบริษัท/โครงการ/ไซต์/ส่วนตัว; ข้อความ realtime, reply, mention, reaction, unread/read receipt และสิทธิ์แยกบริษัท
- ความเสี่ยง: สูง (Messaging/RLS/Tenant isolation/Migration) | หลักฐานทดสอบ: จัดคิวหลังงาน Attendance | Production status: `not_deployed`

### CHAT-002 — ไฟล์ รูป Voice Location และ PWA Notification (10/8/2569)

- สถานะ: `พร้อมทำ` 0% | ขอบเขต: รูป/Selfie/Voice/Location/PDF/Office/แบบก่อสร้าง; private storage, signed URL, scan, version, soft delete และ Push Notification บนมือถือ
- ความเสี่ยง: สูง (Storage/Privacy/PWA/Migration) | หลักฐานทดสอบ: รอ CHAT-001 | Production status: `not_deployed`

### CHAT-003 — รายงาน งาน และการ์ดอนุมัติจากบทสนทนา (10/8/2569)

- สถานะ: `พร้อมทำ` 0% | ขอบเขต: สร้างรายงาน PDF/Excel/CSV; สร้างงาน; การ์ดอนุมัติ Attendance/งาน; AI สรุปและเชื่อมเลขรายการกับ Communication Log
- ความเสี่ยง: สูง (Approval/AI/Reports/Audit) | หลักฐานทดสอบ: รอ CHAT-001 และ CHAT-002 | Production status: `not_deployed`

อัปเดต: 10 สิงหาคม 2569

## กติกาการรับงานจากบทสนทนา

- เมื่อผู้ใช้ยืนยันให้ดำเนินการ ต้องลงรายการในเอกสารนี้ก่อนเริ่มงาน
- ใช้สถานะ: `รอยืนยัน`, `พร้อมทำ`, `กำลังทำ`, `รอตรวจ`, `เสร็จแล้ว`, `ติดปัญหา`
- แต่ละงานต้องมีเลขงาน ขอบเขต ความเสี่ยง หลักฐานทดสอบ วันที่อัปเดต และสถานะ Production
- งานเดิมให้เปลี่ยนสถานะ ห้ามสร้างรายการซ้ำ
- ปิดงานได้เมื่อมีผลทดสอบและยืนยันสถานะ Production ตามขอบเขตงาน

## Dashboard งานค้าง

| สถานะ | จำนวน |
|---|---:|
| พร้อมทำ | 15 |
| กำลังทำ | 1 |
| รอตรวจ | 5 |
| เสร็จแล้ว | 2 |
| ติดปัญหา | 0 |

## รายการงาน

| LINE-QUOTA-001 | ควบคุมโควตา LINE 300 ข้อความ/เดือน | รอตรวจ | สูง | ใช้ Reply เป็นหลัก; Push ตรวจยอดใช้จริงก่อนส่งและคุมระดับ 180/240/270/300; ประเมินผู้รับกลุ่ม 6 คน; รวมแจ้งเตือนลงเวลาหลายคนเป็นข้อความเดียว; quota_blocked บันทึกเป็น skipped เพื่อให้หน้าเว็บรับช่วงต่อ; test:line-quota, Deno check 5 ไฟล์, lint และ build ผ่าน; smoke แบบไม่มีสิทธิ์ตอบ 401 ครบ 4 Functions; อัปเดต 9/8/2569 | Deploy Production แล้ว: attendance-clock, attendance-reminders, health-monitor, line-webhook |
| TELEGRAM-001 | Telegram สำหรับ Admin | รอตรวจ | สูง | 9/8/2569: Migration 019 แก้ ID เป็น `8548319056`; UAT ยืนยัน resolve สิทธิ์ Admin สำเร็จแล้ว (ไม่มีข้อความ unlinked); การส่ง `/status` และ `/tasks` รวมในข้อความเดียวถูกตอบเป็นคำสั่งไม่รู้จักตาม parser ปัจจุบัน; รอทดสอบแยกข้อความคำสั่งละหนึ่งครั้ง หรือพิจารณารองรับหลายคำสั่งต่อข้อความ | Production identity verified / command UAT pending |
| SYS-010 | Automation Worker และ Worker heartbeat | รอตรวจ | สูง | ผู้ใช้ยืนยัน Migration/Deploy 9/8/2569; Migration `202608090014` และ Function `automation-worker` ขึ้น Production; สร้าง atomic claim, lease, heartbeat, stale recovery, run audit และ Secret เฉพาะ; lint/build/Deno check/dry-run ผ่าน; ไม่มี Secret ตอบ 401 ถูกต้อง; รอ authenticated UAT โดยไม่หมุน credential ที่อาจมี Worker ใช้อยู่ | Deploy แล้ว รอ authenticated UAT |
| SYS-011 | Local Codex Automation Runner | รอตรวจ | สูง | 9/8/2569: แก้ fingerprint `local_runner.codex_cli.unsupported_ask_for_approval` โดยเปลี่ยน CLI option เป็น `--approve-for-me` ภายใต้ workspace-write; PowerShell parser ผ่าน; Scheduled Task รอบ 23:14 รับ TEN-002 และ preflight เปลี่ยนเป็น review สำเร็จโดยไม่เปิด Codex งานเสี่ยง; คิวล่าสุด 25 งาน: ready 16, doing 2, review 1, blocked 2, done 4; ยังมี stale active run SYS-008 และ SYS-004 doing ที่ไม่มี worker ต้อง recovery แยก | Local command fixed / safe preflight verified / full Codex run pending |

| เลขงาน | หัวข้อ | สถานะ | ความเสี่ยง | หลักฐาน/เงื่อนไขปิดงาน | Production |
|---|---|---|---|---|---|
| SYS-001 | หน้า Status แบบกราฟิก | เสร็จแล้ว 100% | ต่ำ | ผู้ใช้อนุมัติปิดงาน 10/8/2569 หลังรับทราบขอบเขตและความเสี่ยง โดยเลื่อน Admin Visual UAT ไปตรวจภายหลัง; `/system-health` เป็น route สำหรับ Admin, เมนูจำกัด role Admin และหน้ามีกราฟประวัติรอบตรวจ, สถานะรวม, flow checks, incident/notification tables และการ์ดงานพร้อม progress; `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run test:auth-routing` และ `npm.cmd run test:health-status-report` ผ่าน; Production route บังคับ Login ถูกต้อง; Worker Run ปิดงาน `91470d46-ca7b-4e9f-b37e-7cda7298516c` สำเร็จและไม่มี active run ค้าง | `deployed_visual_uat_deferred_by_user` |
| SYS-002 | Scheduled Monitor เบื้องหลัง 24 ชั่วโมง | เสร็จแล้ว 100% | สูง | `HEALTH_MONITOR_SECRET` และ Vault secret ถูกตั้งโดยไม่เปิดเผยค่า; `health-monitor` และ Migration `202608100011` ขึ้น Production; bootstrap และ scheduled smoke Run `1cf09713-23ad-4a42-b3e6-3522e695fc5a` completed; anonymous HTTP 401; regression, health-report, lint และ build ผ่าน; Cron รอบจริงทำงานและซิงก์แถวเดิมเป็น `done` 100% เวลา 10/8/2569 17:35 น. (Asia/Bangkok); พบและแก้การสร้าง Secret รอบแรกที่ไม่รองรับ PowerShell รุ่นเก่าโดยหมุนค่าใหม่สำเร็จ | `deployed_cron_smoke_passed` |
| SYS-003 | LINE heartbeat ตรวจกรณีส่งแล้วเงียบ | เสร็จแล้ว | สูง | `line-webhook` resolve `company_id` จากกลุ่ม/บัญชีที่ผูกและจำกัด query/insert ตามบริษัท; Deno check, Build, Lint ผ่าน; Production UAT พบ `Webhook-Fix-005` เวลา 9/8/2569 14:17:42 น. สถานะ `processed/completed` ไม่มี Error และข้อความจากกลุ่มกลับเข้าต่อเนื่อง | Deploy ACTIVE v30 และ UAT ผ่าน |
| SYS-004 | ตรวจจับ Error และสร้างงานแก้ไขอัตโนมัติ | รอตรวจ 90% | กลาง | Completed: central error intake, fingerprint deduplication, audited resolution, tenant-safe communication feed, and image optimizer runtime repairs are operational. Current verification on 22/8/2569 refreshed the existing SYS-004 evidence trail in this turn after re-running the same 13 focused regressions (`test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, `test:system-performance-standard`, `test:auth-routing`, `test:chat-launcher-attachment`, and `test:line-attendance-command`); `npm.cmd run lint` and `npm.cmd run build` both passed (exit 0; bundle `SystemHealth-BDQbtKdI.js`); browser verification of `http://127.0.0.1:4173/system-health` and `/` redirected to `/login` and rendered the WisdomAI sign-in page. No duplicate task, migration, deploy, schema/RLS/permission change, secret access, or Production mutation was performed | `deployed_runtime_uat_passed` / focused source verification, lint, build, and real page check passed / review 90% |

> SYS-004 current-turn evidence refresh (22/8/2569): Updated only the existing work-item evidence for the same authoritative row. Revalidated the already-completed SYS-004 scope end-to-end with the focused regressions plus the related routing and webhook checks that are changed in this tree: `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, `test:system-performance-standard`, `test:auth-routing`, `test:chat-launcher-attachment`, and `test:line-attendance-command` all passed; `npm.cmd run lint` and `npm.cmd run build` passed (exit 0; bundle `SystemHealth-Ctq0sOPR.js`); `/system-health` and `/` returned HTTP 200 from the built preview. No duplicate task, migration, deploy, schema/RLS/permission change, secret access, data deletion, or Production mutation was performed.

> SYS-004 latest verification refresh (22/8/2569): Refreshed the same authoritative SYS-004 evidence only, without creating a duplicate task. The current tree re-ran `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, `test:system-performance-standard`, `test:auth-routing`, `test:chat-launcher-attachment`, and `test:line-attendance-command`; `npm.cmd run lint` and `npm.cmd run build` both passed, producing bundle `SystemHealth-SXr3DdOO.js`; a local preview of `/system-health` returned HTTP 200. No migration, deploy, schema/RLS/permission, secret, or Production change was made.

> SYS-004 current-turn evidence refresh (22/8/2569, this turn): Updated the same authoritative SYS-004 evidence trail only. Re-ran `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, `test:system-performance-standard`, `test:auth-routing`, `test:chat-launcher-attachment`, and `test:line-attendance-command`; `npm.cmd run lint` and `npm.cmd run build` passed, producing bundle `SystemHealth-BaurV5Rz.js`; `/system-health` returned HTTP 200 from the built preview. No duplicate task, migration, deploy, schema/RLS/permission change, secret access, data deletion, or Production mutation was performed.

> SYS-004 current-turn evidence refresh (22/8/2569, latest verification): Updated only the existing SYS-004 evidence trail; no duplicate task was created. Re-ran `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, `test:system-performance-standard`, `test:auth-routing`, `test:chat-launcher-attachment`, and `test:line-attendance-command`; `npm.cmd run lint` and `npm.cmd run build` passed (exit 0; bundle `SystemHealth-DyqMInlV.js`); local preview of `/system-health` and `/` returned HTTP 200. No migration, deployment, schema/RLS/permission change, secret access, data deletion, or Production mutation was performed.

> SYS-004 current-turn evidence refresh (22/8/2569, current verification): Updated the same authoritative SYS-004 evidence trail only. Re-ran `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, `test:system-performance-standard`, `test:auth-routing`, `test:chat-launcher-attachment`, and `test:line-attendance-command`; `npm.cmd run lint` and `npm.cmd run build` passed (exit 0; bundle `SystemHealth-BJN_2T9T.js`); local preview of `/system-health` and `/` returned HTTP 200. No duplicate task, migration, deployment, schema/RLS/permission change, secret access, data deletion, or Production mutation was performed.

> SYS-004 current-turn evidence refresh (22/8/2569, real-page verification): Updated only the existing SYS-004 evidence trail in this turn. Re-ran the same 13 focused regressions (`test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, `test:system-performance-standard`, `test:auth-routing`, `test:chat-launcher-attachment`, and `test:line-attendance-command`); `npm.cmd run lint` and `npm.cmd run build` passed (exit 0; bundle `SystemHealth-NBqvE4yE.js`); local preview of `/system-health` and `/` returned HTTP 200. No duplicate task, migration, deployment, schema/RLS/permission change, secret access, data deletion, or Production mutation was performed.

> SYS-004 Production completion 15/8/2569: ผู้ใช้อนุมัติ Migration `202608150001` และ working tree ปัจจุบันทั้งหมด; Migration ขึ้น Production และ local/remote ตรงกัน, `health-monitor` กับ `line-webhook` Deploy สำเร็จ, Vercel deployment `dpl_DUqQHyMPz9BLLiN6Nbhw2fkfbmJE` READY และ alias `wisdomai-react.vercel.app`. Regression, lint, local/Vercel build ผ่าน; `/system-health` HTTP 200; authenticated UI แสดงสถิติ Error, จำนวนระบบตรวจพบ, หลักฐานผู้ใช้ และทะเบียน Error กลางโดยไม่มี load/console error; anonymous Edge smoke ตอบ 401 ถูกต้อง. แถว `public.system_work_items.SYS-004` ปิดเป็น `done` 100% / `deployed_smoke_passed` และไม่มี worker run ค้าง.


> SYS-004 resolution audit 15/8/2569: ตรวจ Production พบ Error เปิด 3 รายการ ได้แก่ health `employee_readiness` 1 รายการ และ Web error รุ่นก่อน 2 รายการที่ Source แก้แล้วแต่ยังไม่มีวงจรปิด incident ทำให้ SYS-004 ถูกเปิดกลับอย่างถูกต้อง. เตรียม Migration `202608150002_system_error_resolution_workflow.sql` เพิ่มเหตุผล/ผู้ปิด/เวลาปิด, RPC สำหรับ Admin resolve/dismiss แบบ tenant-safe, automatic resolve เมื่อ Health กลับมาปกติ และ guard ห้ามปิด SYS-004 หากยังมี Error เปิด; หน้า System Health เพิ่มปุ่มจัดการพร้อมบังคับเหตุผล. `test:system-error-intake`, lint และ build ผ่าน. ยังไม่ได้รัน Migration หรือ Deploy; รออนุมัติ Production.

> SYS-004 Production resolution workflow 15/8/2569: ผู้ใช้อนุมัติ Migration `202608150002`; Production migration สำเร็จและ local/remote ตรงกัน, `health-monitor` Deploy สำเร็จ, Vercel deployment `FYdUkhSENNCzQgsY2wcTpcdeCaYK` READY และ alias `wisdomai-react.vercel.app`. Regression, lint, local/Vercel build ผ่าน; Web HTTP 200 และ anonymous health-monitor ตอบ 401 ถูกต้อง. ระบบปิด Error แบบมี Audit และ automatic health recovery ใช้งานแล้ว. SYS-004 อยู่ `review` 98% / `deployed_smoke_passed_uat_pending` เนื่องจาก incident จริง `health:workforce:employee_readiness` ยังต้องแก้ข้อมูลพนักงานและตรวจซ้ำ; ไม่ปิดเป็น done ขณะยังมี Error เปิด.

> SYS-004 readiness detail hotfix 15/8/2569: ผู้ใช้อนุมัติให้แสดงชื่อ รหัสพนักงาน และรายการข้อมูลที่ขาดเฉพาะผู้ดูแลบริษัท. ปรับ `health-monitor` จาก count-only ให้ส่ง `not_ready_people` พร้อมชื่อ/รหัสและ missing fields (ชื่อ, ข้อมูลจ้างงาน, ค่าจ้าง, ตารางเวลา, ไซต์) ภายใต้ company scope เดิม; `test:system-error-intake`, `test:workforce-readiness-health`, lint และ build ผ่าน; Deploy Production สำเร็จ และ anonymous smoke ตอบ 401 ยืนยันว่าไม่เปิดเผยต่อผู้ไม่มีสิทธิ์. รอ Admin กด “ตรวจทันที” เพื่อยืนยันรายชื่อจริงบนหน้า System Health.

> SYS-004 source recheck 15/8/2569: ตรวจ working tree โดยไม่รัน Migration หรือ Deploy; `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, `npm.cmd run lint` และ `npm.cmd run build` ผ่านทั้งหมด (exit 0; bundle `SystemHealth-CZimmJEB.js`) ยืนยัน tenant-scoped intake, fingerprint dedupe, การเชื่อมหลักฐาน screenshot จาก LINE เข้ากับ incident เดิม, audited resolve/dismiss source workflow, ทะเบียนปัญหากลาง, tenant-safe communication feed, LINE webhook intake และ readiness diagnosis ภายใต้ company scope. อัปเดต evidence ของ SYS-004 รายการเดิมให้ตรงกับสถานะ `review` 90% และคง Production เดิมตามหลักฐาน deployment ข้างต้น โดยไม่แก้ schema/RLS/permission และไม่สร้างงานซ้ำ.
| SYS-005 | ศูนย์สั่งงานผ่านมือถือและคอมพิวเตอร์ | รอตรวจ 70% | กลาง | 11/8/2569 ตรวจและเติม Source ของศูนย์สั่งงาน: สร้างคำสั่งผ่าน `create_system_work_item` โดยผูกบริษัทปัจจุบัน, ติดตามด้วยสถานะ/ตัวกรอง/ความก้าวหน้า, เปิด Timeline จาก `system_work_item_events` และอนุมัติหรือไม่อนุมัติพร้อมเหตุผลที่ trigger เก็บ actor/เวลาใน audit; `npm.cmd run test:work-command-center` และ `npm.cmd run build` ผ่าน, lint ผ่านโดยมี warning เดิม 1 จุดใน Reports; ไม่รัน Migration และไม่เปลี่ยนสิทธิ์ | Source พร้อมตรวจ / ยังไม่ Deploy และยังไม่ทำ Production UAT |
| SYS-006 | ตรวจงานกำลังทำและงานค้างด้วย Worker heartbeat | รอตรวจ | กลาง | Migration `202608090020_recover_stale_automation_work.sql` ขึ้น Production: ปิด running heartbeat เกิน 10 นาทีเป็น expired, คืน orphan/stale doing เป็น ready, retry งานที่ล้มจาก CLI option เดิม และเรียก recovery ก่อนทุก claim; Production UAT หลัง deploy ไม่มี active run ค้าง, SYS-004/SYS-008 กลับ ready, SYS-005/SYS-009 พร้อม retry; ตรวจยืนยัน 10/8/2569 ที่ progress 95%: regression test ครอบคลุม stale run, orphan doing, active-run guard, retry CLI option, recovery-before-claim และ runner heartbeat ผ่าน พร้อม PowerShell parser, `npm.cmd run lint` และ `npm.cmd run build`; runner ไม่มี `--ask-for-approval`/`--sandbox` และใช้ `--approve-for-me`; รอบนี้ไม่มี Migration, Deploy หรือแก้ Production | Deploy แล้ว / recovery UAT passed / source verification passed / รอตรวจรอบ Scheduled Task ต่อเนื่อง |
| SYS-007 | ส่งรายงาน Status เข้ากลุ่ม LINE ทดสอบโปรแกรม | รอตรวจ 98% | กลาง | 10/8/2569 ผู้ใช้อนุมัติ Deploy Source fix และ LINE UAT; `test:health-status-report`, `test:line-quota`, lint และ build ผ่าน exit 0 (bundle `SystemHealth-DStCaHcW.js`); Deploy `health-monitor` Production สำเร็จโดยคง `verify_jwt=false` และตรวจ Admin ภายใน Function; anonymous smoke ตอบ HTTP 401 ตามคาด; เหลือ Admin กด `ส่ง Status เข้า LINE` หนึ่งครั้งเพื่อยืนยันข้อความจริง กลุ่มปลายทาง และการกันส่งซ้ำ 5 นาที; blocker `sys007.admin_line_uat.pending` | Source fix Deploy แล้ว / รอ Admin LINE UAT |
| SYS-008 | ศูนย์สั่งงานผ่าน LINE และ Voice | รอตรวจ 90% | สูง | 10/8/2569 ตรวจ Source ของ LINE จริงและตรวจซ้ำในรอบ SYS-008 ล่าสุด: `line-webhook` ดาวน์โหลด audio, ถอดเสียงไทยด้วย Gemini, บันทึก transcript เป็นคำสั่งสถานะ `awaiting_confirmation`, แสดงข้อความถอดเสียงให้ผู้สั่งกดยืนยัน/ยกเลิก และเมื่อยืนยันจึงเข้าคิวหรือส่งขออนุมัติตามความเสี่ยง พร้อม audit events; รอบตรวจนี้ `npm.cmd run lint`, `npm.cmd run build` และ `npm.cmd run test:line-quota` ผ่าน (exit 0); หลักฐานนี้เป็น static/local validation เท่านั้น จึงยังไม่เพิ่มความคืบหน้าและยังต้องรอ Voice UAT จาก LINE จริงตาม Scope | Deploy แล้ว / รอ LINE Voice UAT จริง |
| SYS-013 | Unified Voice, Chat & Codex Approval Command Center | พร้อมทำ 0% | สูง | ใช้ Web Chat เป็นช่องทางหลักเมื่ออยู่ใน WisdomAI, Telegram เป็นช่องทางมือถือ และ LINE สำหรับพนักงาน/หน้างาน; ทุกข้อความ เสียง ไฟล์ และคำอนุมัติต้องเข้า Unified Command Inbox และสร้าง Work Item ชุดเดียว พร้อม correlation ID, การทวนคำสั่ง, ตรวจตัวตน/บริษัท/สิทธิ์ และ Audit; Codex Worker วิเคราะห์ แก้ Code รัน lint/build/tests และส่งหลักฐานกลับได้ แต่ห้ามอนุมัติงานของตนเอง; Security, RLS, Migration, Secret, การลบข้อมูล และ Production ต้องให้เจ้าของระบบอนุมัติ พร้อม Preview/rollback/post-deploy monitor | ยังไม่ Deploy / รอออกแบบ Contract, Permission และ Approval Gate |
| OPS-001 | บันทึกงานที่ยืนยันลง List และรายงาน Status | เสร็จแล้ว | ต่ำ | Automation และเอกสารใช้ workflow เดียวกัน | ใช้งานแล้ว |
| AUD-001 | ตรวจผลกระทบ Multi-company Migration ทุกโมดูล | รอตรวจ 92% | สูง | Tenant regression ทุกชุดผ่าน; TEN-007/TEN-010 ทำ Production แล้ว; TEN-011 แก้ company-scoped LINE identity ด้วย Migration `202608110021` และ Deploy `line-webhook` แล้ว รอ LINE link UAT; authenticated negative UAT ทุกโมดูลยังเป็น TEN-012 | Static audit ผ่าน / รอ TEN-011 UAT และ TEN-012 |
| TEN-001 | ซ่อม Attendance Edge ให้ส่งและกรองบริษัทครบ | รอตรวจ | วิกฤต | เติม active company/membership, กรอง employment/site/assignment/session/config ตามบริษัท และส่ง `company_id` ใน session/notification; Deno check, Lint, Build ผ่าน; Deploy 9/8/2569 | Deploy แล้ว รอ UAT เข้า/ออก/GPS/เครื่องร่วม/ลงซ้ำ |
| TEN-002 | ซ่อม Attendance Reminder และรอบ 20:00/00:00/07:00 | เสร็จแล้ว 100% | วิกฤต | 10/8/2569 ตรวจ Production ซ้ำ: Migration `202608090011_secure_attendance_reminder_cron.sql` อยู่ใน remote แล้ว; Cron เรียกผ่าน Vault-backed `attendance_monitor_secret`; Function แยก Cron secret กับ Admin User JWT และตรวจ company membership role; คำขอไม่มีสิทธิ์ตอบ HTTP 401; lint/build ผ่าน; ไม่เปิดเผย Secret; ผู้ใช้ยืนยันปิดงาน | Deploy แล้ว / ปิดงานแล้ว |
| TEN-003 | ซ่อม Drawing AI Edge และผลวิเคราะห์ทุกตาราง | รอตรวจ 95% | สูง | Source audit และ revalidation 11/8/2569: หน้า Drawing AI กรอง projects/jobs/runs/module runs/sheets/items ด้วย active `company_id`; writes ของ job/project/ground truth/score/scope/dependency/update ระบุหรือกรองบริษัท; งดอ่าน leaderboard/learning aggregate views ที่ยังไม่มี company dimension; Edge ตรวจ active membership และกรอง/เขียน company ครบ; `npm.cmd run test:drawing-ai-tenant`, `npm.cmd run lint` (0 errors, 1 warning เดิมใน Reports) และ `npm.cmd run build` ผ่านซ้ำ; Production เดิม deploy 10/8/2569 และยังเหลือ Admin UAT 2 บริษัท | Source verified / ไม่ deploy รอบนี้ / รอ UAT 2 บริษัท |
| TEN-004 | ซ่อมการเพิ่มพนักงานและผูกสมาชิกบริษัท | เสร็จแล้ว 100% | วิกฤต | Production 10/8/2569: Migration `202608100002_tenant_safe_employee_bootstrap.sql` สำเร็จและปรากฏในประวัติ remote; Deploy `create-employee` สำเร็จที่ project `xkieyqixlufjqructjkr`; smoke แบบไม่มีสิทธิ์ตอบ HTTP 401; `git diff --check`, lint และ build ผ่าน; Admin ยืนยันสร้างพนักงานจริงบน Production สำเร็จแล้ว | Deploy แล้ว / UAT ผ่าน |
| TEN-005 | ป้องกันจัดการ/ลบพนักงานข้ามบริษัท | เสร็จแล้ว 100% | วิกฤต | 10/8/2569 Migration `202608100003` และ `manage-employee` v15 ขึ้น Production สำเร็จหลังผู้ใช้ยืนยันคง `verify_jwt=false`; Function ตรวจ Bearer token, active company, company_admin/executive และ target membership ภายใน; archive/reactivate จำกัด membership, employment, site และ audit ตามบริษัทโดยไม่ ban Auth global; delete ปฏิเสธเมื่อมีบริษัทอื่นหรือประวัติ; `test:employee-tenant`, lint และ build ผ่าน; Production anonymous smoke ตอบ HTTP 401 `Unauthorized` | Deploy แล้ว / ACTIVE v15 / smoke ผ่าน |
| TEN-006 | แยกตาราง Config แบบ singleton ต่อบริษัท | เสร็จแล้ว 100% | สูง | 10/8/2569 Migration `202608100005` ขึ้น Production และ local/remote ตรงกัน; `test:tenant-config`, lint และ build ผ่าน; Deploy working tree ที่ผู้ใช้อนุมัติขึ้น Vercel สำเร็จและ alias เป็น `https://wisdomai-react.vercel.app`; smoke test `/login` และ `/workforce-setup` ตอบ HTTP 200 | Database + Frontend deployed / smoke passed |
| TEN-007 | ปรับโครงสร้างการจ้างงานพนักงานหลายบริษัท | เสร็จแล้ว 100% | สูง | Migration `202608110015` เปลี่ยนคีย์ข้อมูลจ้างงานเป็น `(company_id, profile_id)` โดยรักษาข้อมูลเดิม; Employee, Reports, Workforce Setup และ Project Controls อ่าน/เขียนข้อมูลจ้างตามบริษัทปัจจุบัน; create-employee ใช้ composite conflict target; tenant regression, lint และ build ผ่าน; หน้า Production 4 เส้นทาง HTTP 200 และ anonymous create-employee ตอบ 401 | Migration + Edge + Frontend deployed / smoke passed |
| TEN-008 | ตรวจและอุด Tenant boundary ใน Security Definer RPC/Trigger | เสร็จแล้ว 100% | วิกฤต | 10/8/2569 apply `202608100004_security_definer_tenant_boundaries.sql` ขึ้น Production สำเร็จและตรวจ migration list พบ local/remote ตรงกัน; ป้องกัน FK ข้ามบริษัทสำหรับพนักงาน โครงการ ไซต์ BOQ บัญชี สต็อก ผู้รับเหมา Cost Code และรอบจ่าย; `test:tenant-rpc`, `test:employee-tenant`, lint และ build ผ่าน | Production / Migration applied และ verified |
| TEN-009 | แยก Storage path และสิทธิ์ไฟล์ต่อบริษัท | พร้อมทำ | วิกฤต | `line-attachments`, `drawing-ai`, `boq-imports` ยังมี policy/พาธที่ไม่บังคับ company namespace; restrictive policy ปัจจุบันคุมเฉพาะ storage เอกสารพนักงานบาง bucket | รอ Migration, path compatibility และ Storage UAT |
| TEN-010 | แยก Health Monitor/Status/Telegram ปลายทางต่อบริษัท | เสร็จแล้ว 100% | สูง | Migration `202608110017` แยก settings/checks/incidents/runs/notifications เป็น global หรือ company scope พร้อม RLS ผู้จัดการบริษัท; หน้า Status และ runner กรอง active company; Function ตรวจ membership และกรองข้อมูลตามบริษัท; tests, lint/build และ Production smoke ผ่าน; ผู้ใช้อนุมัติปิดและเลื่อน authenticated two-company/Telegram destination UAT ไปตรวจภายหลัง 11/8/2569 | `deployed_uat_deferred_by_user` |
| TEN-011 | ซ่อม LINE Account Link ที่เหลือและรองรับสมาชิกหลายบริษัท | เสร็จแล้ว 100% | สูง | Migration `202608110021`, `202608110028` และ audit repair `202608110029` ขึ้น Production; UAT ผูกกลุ่ม “ทีมบ่าว” กับ TEC สำเร็จและมี `line_group_company_assigned` audit 2 ครั้ง โดยครั้งล่าสุด 11/8/2569 09:16 น.; transaction ไม่มีข้อมูลค้างบางส่วน | platform_line_group_mapping_uat_passed |
| TEN-012 | ทำ Multi-company Regression Test ครบทุกโมดูลก่อน Deploy | พร้อมทำ | สูง | เพิ่ม test matrix Frontend/Edge/RPC/DB/Storage/Automation: Projects, BOQ, Drawing, LINE, Attendance, Employee, Leave/OT/Payroll, Contractor, Accounting, Reports, Export, Health; ต้องทดสอบอย่างน้อย 2 บริษัทและ negative cross-tenant | รอหลัง TEN-001–011 |

### อัปเดต SYS-007 — 10/8/2569

- สถานะ: รอตรวจ 99%
- ขอบเขต: ย้ายข้อความสถานะระบบ เหตุขัดข้อง งานค้าง และคำขออนุมัติสำหรับ Admin จาก LINE ไป Telegram; LINE คงไว้สำหรับรับคำสั่ง/ลงเวลาและตอบกลับพนักงานเท่านั้น
- ความเสี่ยง: กลาง — ต้องรักษาการแยกข้อมูลและผู้รับตามบริษัท
- หลักฐานทดสอบ: `test:health-status-report`, `test:line-quota`, lint และ build ผ่าน; Deploy `health-monitor` Production สำเร็จ; anonymous smoke ตอบ HTTP 401 ตามคาด; Vercel Production deployment `dpl_J4hbbxHzwyCVaf9B9iFbqpCdVJBe` Ready และ alias `wisdomai-react.vercel.app`; `/system-health` HTTP 200, app root พบครบ, lazy bundle `SystemHealth-DeGF1ZhP.js` พบข้อความ Telegram และไม่พบปุ่ม LINE เดิม; รอ authenticated Telegram UAT หนึ่งครั้ง
- Production status: telegram_admin_backend_and_frontend_deployed_uat_pending

### อัปเดต SYS-007 — 10/8/2569 (Telegram stale-chat recovery)

- สถานะ: `เสร็จแล้ว 100%`
- ขอบเขต: ปิดห้อง Telegram ที่ Bot ถูกนำออก/หาไม่พบโดยอัตโนมัติ, ลงทะเบียนห้องปัจจุบันจากข้อความของ Admin ที่ผูกสิทธิ์, รองรับ `my_chat_member` และแสดงผลสำเร็จบางส่วนโดยไม่ทำให้ห้องที่ส่งสำเร็จถูกนับเป็นความล้มเหลวทั้งชุด
- ความเสี่ยง: ปานกลาง — เปลี่ยน Edge Function `health-monitor` และ `telegram-admin` แต่ไม่เปลี่ยน schema หรือ secret
- หลักฐาน: Production UAT เดิมแสดง `ส่งรายงาน Telegram ไม่สำเร็จ`; เพิ่ม terminal chat deactivation, active-chat upsert และ `my_chat_member`; tests, lint และ build ผ่าน; Deploy `health-monitor` และ `telegram-admin` สำเร็จ; Webhook URL ตรง, pending 0, ไม่มี error; anonymous smoke ทั้งสอง endpoint ตอบ 401 ตามคาด
- วันที่อัปเดต: 10/8/2569
- ผล UAT ล่าสุด: ผู้ใช้ยืนยันวันที่ 10/8/2569 ว่าระบบรับ Telegram เข้ามาได้แล้ว
- Production status: `telegram_admin_deployed_uat_passed`

## ข้อกำหนด SYS-005: ศูนย์สั่งงาน

- เมนู `ระบบตรวจสอบ → ศูนย์สั่งงาน`
- ฟอร์มกระชับบนมือถือ: หัวข้องาน, รายละเอียด, ประเภท, ความสำคัญ, ผู้รับผิดชอบ, กำหนดเสร็จ และไฟล์แนบ
- ปุ่มคำสั่งสำเร็จรูป: ตรวจ Error, ตรวจ LINE, ตรวจลงเวลา, ทดสอบระบบ, Deploy และสร้างรายงาน
- ทุกคำสั่งสร้างเลขงานและเข้าสู่ List งานค้างทันที
- แสดงขั้นตอน `รอยืนยัน → พร้อมทำ → กำลังทำ → รอตรวจ → เสร็จแล้ว/ติดปัญหา`
- งานเสี่ยง เช่น Migration, Secret, สิทธิ์ และการลบข้อมูล ต้องขออนุมัติเพิ่มก่อนดำเนินการ
- แยกข้อมูลตามบริษัทและสิทธิ์ พร้อม audit log ผู้สั่ง ผู้อนุมัติ และเวลาที่เปลี่ยนสถานะ
- รองรับการตอบกลับผลในหน้าเว็บและแจ้ง LINE ตาม Config

## ข้อกำหนด SYS-006: รู้ว่างานกำลังทำหรือค้าง

- ทุก execution ต้องมี `run_id`, `work_item_id`, worker, started_at, heartbeat_at, current_step, progress และ next_run_at
- Worker ต่ออายุ heartbeat ระหว่างทำงาน และบันทึกผลแต่ละขั้นแบบ append-only
- สถานะหน้าจอ: `รอคิว`, `กำลังทำ`, `รออนุมัติ`, `รอตรวจ`, `ค้าง`, `ล้มเหลว`, `เสร็จแล้ว`
- ค่าเริ่มต้น: ไม่มี heartbeat 10 นาทีเป็น `เฝ้าระวัง`; 30 นาทีเป็น `ค้าง` โดยปรับได้ใน Config
- แสดงเวลาที่ทำมาแล้ว เวลาประมาณการ ขั้นตอนล่าสุด Error ล่าสุด และปุ่มดู log
- ป้องกัน Worker สองตัวทำงานเดียวกันด้วย lease/lock และ idempotency key
- งานค้างต้องแจ้ง LINE หนึ่งครั้ง และเตือนซ้ำตาม Config ไม่ส่งทุก heartbeat
- ถ้า Worker กลับมาทำงาน ให้เปลี่ยนเป็น `กำลังทำ` และบันทึก recovery; ถ้าเกิน retry ให้ `ติดปัญหา`

## ข้อกำหนด SYS-008: สั่งงานผ่าน LINE และ Voice

### ช่องทางรับคำสั่ง

- รับข้อความในกลุ่ม LINE และข้อความส่วนตัวตาม Config
- รับ Voice/Audio จาก LINE แล้วถอดเสียงเป็นภาษาไทย พร้อมเก็บไฟล์อ้างอิงตามระยะเวลาที่กำหนด
- รองรับคำสั่ง: สร้างงาน, สอบถามปัญหา, ขอวิเคราะห์, ขอแก้ไข, อนุมัติ/ปฏิเสธ, ขอข้อมูลเพิ่ม, เปลี่ยนผู้รับผิดชอบ, ปรับความสำคัญ, เลื่อนคิว และยกเลิก

### ขั้นตอนยืนยันก่อนสร้างหรือเปลี่ยนงาน

1. ตรวจ LINE user ID, บริษัท, กลุ่ม, โครงการ และสิทธิ์ของผู้สั่ง
2. แปลงข้อความหรือ Voice เป็นโครงสร้างงาน: หัวข้อ, รายละเอียด, ประเภท, โครงการ, ความสำคัญ, กำหนดเสร็จ และผลกระทบ
3. ตอบกลับเพื่อทวนคำสั่ง พร้อมเลขอ้างอิงชั่วคราวและส่วนที่ AI ไม่มั่นใจ
4. ผู้ใช้ตอบ `ยืนยัน`, `แก้ไข ...` หรือ `ยกเลิก`
5. เมื่อยืนยันแล้วจึงสร้าง/อัปเดตงานจริง และส่งเลขงานกับลิงก์ Status กลับ LINE
6. คำสั่งที่มีผลเสี่ยงต้องส่งให้ผู้มีสิทธิ์อนุมัติอีกชั้นก่อน Worker เริ่มทำ

### การสอบถามและการตัดสินใจ

- หากข้อมูลไม่ครบ ระบบถามเฉพาะช่องที่ขาด ไม่สร้างงานเดาเอง
- เมื่อพบปัญหา ส่งสาเหตุที่คาดการณ์ หลักฐาน ผลกระทบ ทางเลือกแก้ไข ความเสี่ยง และคำแนะนำ
- ผู้มีสิทธิ์ตอบ `อนุมัติ SYS-xxx`, `ปฏิเสธ SYS-xxx เหตุผล...`, `ขอข้อมูล SYS-xxx` หรือกดปุ่ม Postback
- ทุกการตัดสินใจต้องบันทึก LINE user ID, เวลา, คำสั่งเดิม, คำตอบ และ audit log

### ลำดับความสำคัญและคิว

- ระดับ: `ฉุกเฉิน`, `สูง`, `ปกติ`, `ต่ำ` พร้อม SLA และเหตุผลที่ปรับ
- `ฉุกเฉิน`: ระบบล่ม, ความปลอดภัย, ข้อมูลสูญหาย, ลงเวลาทั้งไซต์ไม่ได้ หรือกระทบ Production วงกว้าง
- ผู้มีสิทธิ์สั่ง `เร่ง SYS-xxx`, `ลดความสำคัญ SYS-xxx`, `ทำก่อน SYS-xxx` หรือกำหนด deadline
- Scheduler คำนวณคิวจาก severity + business impact + SLA + dependency + อายุงาน + การอนุมัติ
- การเร่งงานห้ามแทรกงาน Migration/ลบข้อมูล/Secret/สิทธิ์โดยข้ามขั้นอนุมัติ
- LINE ต้องตอบตำแหน่งคิว เวลาประมาณการเริ่ม/เสร็จ และแจ้งเมื่อคิวเปลี่ยน

### Voice safety

- แสดงข้อความถอดเสียงให้ยืนยันเสมอ ห้ามสั่ง Deploy, ลบข้อมูล หรืออนุมัติงานเสี่ยงจากเสียงครั้งเดียว
- หากชื่อโครงการ คน ตัวเลข วันเวลา หรือคำสั่งสำคัญไม่ชัด ต้องถามซ้ำ
- เก็บ confidence, model, ภาษา และการแก้ข้อความของผู้ใช้เพื่อปรับปรุงความแม่นยำ โดยไม่ใช้ข้อมูลข้ามบริษัท

## ข้อกำหนด SYS-004: Error → งานแก้ไข → รายงานผล

1. รับ Error จาก Frontend, Edge Function, Database, LINE Webhook, Attendance, BOQ และงาน Automation
2. สร้าง `fingerprint` จากบริษัท + module + error code + ตำแหน่ง เพื่อรวม Error เดิมเป็นงานเดียว
3. เก็บจำนวนครั้ง, ครั้งแรก, ครั้งล่าสุด, ผู้ใช้/อุปกรณ์ที่ได้รับผลกระทบ และ Error reference โดยไม่เก็บ Secret
4. จัดระดับ `critical`, `high`, `medium`, `low` และกำหนด SLA
5. สร้างงานสถานะ `พร้อมทำ` อัตโนมัติ หรือเพิ่มจำนวนครั้งในงานเดิม
6. เมื่อเริ่มแก้ให้เป็น `กำลังทำ`; ผ่าน Build/Test เป็น `รอตรวจ`; Production ผ่านเป็น `เสร็จแล้ว`
7. Error เดิมเกิดซ้ำหลังปิดงาน ให้เปิดงานเดิมใหม่และบันทึก regression
8. หน้า Status ต้องแสดง Error ใหม่, Error ซ้ำ, งานค้าง, อายุงาน, SLA เกินกำหนด และผลแก้ไขล่าสุด
9. LINE แจ้งเฉพาะตามระดับและกติกาป้องกันการส่งซ้ำใน Config

### งานย่อยที่ยืนยันแล้ว

| เลขงาน | แหล่ง Error | สิ่งที่ต้องตรวจจับ | สถานะ | เกณฑ์ปิดงาน |
|---|---|---|---|---|
| ERR-001 | Browser/Frontend | `window.error`, `unhandledrejection`, React/Route Error, Chunk load, หน้า blank และ resource load | พร้อมทำ | ทุก Error มี reference และทดสอบจากมือถือ/คอมพิวเตอร์ |
| ERR-002 | API/Network | Fetch/Supabase/Edge Function non-2xx, timeout, offline, CORS, JWT และ rate limit รวมทั้ง Error ที่ถูก catch แล้ว | พร้อมทำ | request wrapper บันทึก endpoint แบบปกปิดข้อมูล, status, latency และ retry result |
| ERR-003 | Edge Function/LINE | Function exception, webhook signature, token, reply/push, attachment, AI analysis และ ingestion pipeline | พร้อมทำ | แสดงขั้นตอนที่ล้มเหลว เวลาสำเร็จล่าสุด และตรวจกรณีส่งแล้วเงียบ |
| ERR-004 | Database/Data | SQL/RPC/RLS/FK/schema mismatch, duplicate, missing relation และข้อมูลไม่ครบ | พร้อมทำ | จัดกลุ่มตาม table/function และแยก permission error ออกจาก data error |
| ERR-005 | Business workflow | ลงเวลา, GPS, พนักงาน–ไซต์, ลา/OT/ค่าจ้าง, BOQ/PDF/Excel และ approval | พร้อมทำ | เก็บ business error code, input ที่ปลอดภัย และผลกระทบต่อกระบวนการ |
| ERR-006 | Automation/Deploy | Build, Lint, Test, Migration, Vercel, Supabase deploy, scheduled monitor และ smoke test | พร้อมทำ | ทุก run มีผลลัพธ์, log reference, commit/deployment และ rollback status |
| ERR-007 | Error intelligence | fingerprint, dedupe, severity, root-cause suggestion, fix recommendation, confidence และ regression | พร้อมทำ | Error ซ้ำไม่สร้างงานซ้ำ และบทวิเคราะห์ระบุหลักฐาน/ความไม่แน่นอน |
| ERR-008 | Resolution workflow | สร้างงาน, มอบหมาย, SLA, แก้ Code, ทดสอบ, Deploy, ตรวจซ้ำ, ปิดหรือเปิดใหม่ | พร้อมทำ | สถานะและ audit log ครบวงจร พร้อมแสดงในหน้า Status |

### Error ที่กำลังติดตาม: LINE inbound silence / outbound quota

| Fingerprint | Severity | ครั้งแรก | ล่าสุด | จำนวน | ผลกระทบ | ผู้รับผิดชอบ | SLA | สถานะ | หลักฐาน | Production |
|---|---|---|---|---:|---|---|---|---|---|---|
| `line:webhook:company-id-not-null-after-tenant-migration` | วิกฤต | 9/8/2569 07:44 | 9/8/2569 14:17 | 5 reports | Voice/คำสั่ง/รูป/สรุป LINE ทุกกลุ่มไม่เริ่มทำงานและ Function ตอบ non-2xx | Edge/Database + ระบบ SYS-003 | 1 ชม. | แก้ไขแล้ว | Root cause คือ tenant migration เพิ่ม `company_id NOT NULL`; แก้ Function ให้ resolve บริษัทจาก `line_groups`, `line_senders` หรือ `employee_line_accounts`, เติม company ใน ingestion/message/project/summary/attachment/AI/accounting writes และกรอง query ข้ามบริษัท; Deno/Build/Lint ผ่าน; UAT `Webhook-Fix-005` และข้อความกลุ่มประมวลผลครบ ไม่มี Error | `line-webhook` ACTIVE v30; Production UAT ผ่าน |
| `line:push:monthly-limit:429` | สูง | 9/8/2569 12:15 | 9/8/2569 12:47 | 2 | ส่ง Status/แจ้งเตือนแบบ Push ไม่ได้ | เจ้าของ LINE OA | 4 ชม. | ติดปัญหาโควตา | LINE API ตอบ `429 monthly limit` สองครั้ง; หน้า LINE Monitor แยก inbound/outbound และแสดง 429 แล้ว | ระบบตรวจจับ Deploy แล้ว แต่ Production ส่งออกยังล้มเหลวจนกว่าโควตาพร้อม |

### ผลกระทบที่ต้องตรวจต่อจาก Multi-company Migration (ตรวจโค้ด 9/8/2569)

| Fingerprint | ระดับ | โมดูล | สิ่งที่พบ | สถานะ |
|---|---|---|---|---|
| `tenant:not-null:attendance-edge-writes` | วิกฤต | Attendance | `attendance-clock` ยัง insert `attendance_sessions` และ `attendance_notifications` โดยไม่ระบุ `company_id`; Service Role ไม่สามารถอาศัย active company ของผู้ใช้ได้ | พบจาก Code audit รอแก้และ UAT เข้า/ออก/GPS |
| `tenant:not-null:attendance-reminder-writes` | สูง | Attendance Reminder | `attendance-reminders` insert `attendance_reminder_events` โดยไม่ระบุ `company_id` และอ่านรายการข้ามบริษัทโดยไม่กรอง tenant | พบจาก Code audit รอแก้และทดสอบรอบ 20:00/00:00/07:00 |
| `tenant:not-null:drawing-ai-writes` | สูง | Drawing AI | แก้ source แล้ว: Edge writes ของ run/module/sheet/item/project ระบุ `company_id`; frontend reads/writes ของ tenant tables ใช้ active company และปิด aggregate views ที่ไม่มี company dimension; static regression/lint/build ผ่าน | Source verified 11/8/2569; รอ authenticated UAT ไฟล์จริง 2 บริษัท |
| `tenant:scope:health-monitor-service-role` | สูง | Health Monitor | การตรวจด้วย Service Role อ่านหลายตารางโดยไม่กรอง `company_id` ทำให้ผลรวมหลายบริษัทปะปน แม้บางจุดไม่เกิด NOT NULL error | พบจาก Code audit รอออกแบบผลตรวจแยกบริษัท |
| `tenant:not-null:line-account-link` | สูง | LINE Account Link | เส้นทาง fallback สองจุด upsert `employee_line_accounts` โดยไม่ระบุ `company_id`; ข้อความทั่วไปผ่านแล้วแต่การผูกบัญชีอาจล้ม | พบจาก Code audit รอแก้และ UAT ผูกบัญชี |

### SYS-004 — กลุ่ม Error จาก Tenant Impact Audit

| Fingerprint | Severity | ครั้งแรก/ล่าสุด | จำนวน | ผลกระทบ | ผู้รับผิดชอบ | SLA | สถานะ/Production |
|---|---|---|---:|---|---|---|---|
| `tenant:attendance:edge-company-missing` | วิกฤต | 9/8/2569 | 2 write paths | ลงเวลา/แจ้งเตือนอาจบันทึกล้ม | Attendance/DB | 1 ชม.หลังเริ่มแก้ | พร้อมทำ/ยังไม่ Deploy |
| `tenant:attendance:reminder-unscoped` | วิกฤต | 9/8/2569 | 5 query/write paths | แจ้งผิดบริษัทหรือไม่สร้าง event | Automation/Attendance | 1 ชม.หลังเริ่มแก้ | พร้อมทำ/ยังไม่ Deploy |
| `tenant:drawing-ai:service-writes-unscoped` | สูง | 9/8/2569 | อย่างน้อย 6 write paths | วิเคราะห์แบบ/สร้างโครงการล้ม หรืออ่านรวมบริษัท | Drawing AI | 4 ชม.หลังเริ่มแก้ | พร้อมทำ/ยังไม่ Deploy |
| `tenant:employee:create-membership-missing` | วิกฤต | 9/8/2569 | 3 จุด | เพิ่มพนักงานล้มและไม่ผูกบริษัท | Employee/Auth | 1 ชม.หลังเริ่มแก้ | พร้อมทำ/ยังไม่ Deploy |
| `tenant:employee:manage-cross-company-auth` | วิกฤต | 9/8/2569 | 1 privileged workflow | เสี่ยงระงับ/ลบบัญชีต่างบริษัท | Security/Auth | 1 ชม.หลังเริ่มแก้ | พร้อมทำ/ยังไม่ Deploy |
| `tenant:config:singleton-primary-key` | สูง | 9/8/2569 | 3 config tables | บริษัทใหม่ไม่มีค่าตั้งค่าแยก | DB/Config | 4 ชม.หลังเริ่มแก้ | พร้อมทำ/รอ Migration |
| `tenant:employment:profile-global-key` | สูง | 9/8/2569 | 1 core table + downstream | คนเดียวมีเงื่อนไขจ้างต่างบริษัทไม่ได้ | Workforce/Payroll | 4 ชม.หลังเริ่มแก้ | พร้อมทำ/รอ Migration |
| `tenant:rpc:security-definer-target-unscoped` | วิกฤต | 9/8/2569 | หลาย RPC รุ่นเดิม | target UUID และข้อมูลอ้างอิงต้องตรวจ tenant ซ้ำ | DB/Security | 1 ชม.หลังเริ่มแก้ | พร้อมทำ/รอ Migration |
| `tenant:storage:path-policy-unscoped` | วิกฤต | 9/8/2569 | 3 buckets หลัก | เสี่ยงมองเห็น/อ้างไฟล์ข้ามบริษัท | Storage/Security | 1 ชม.หลังเริ่มแก้ | พร้อมทำ/รอ Migration |
| `tenant:health-monitor:global-state` | สูง | 9/8/2569 | 5 health tables | Status/incident/ปลายทาง LINE ปะปนบริษัท | Monitoring | 4 ชม.หลังเริ่มแก้ | พร้อมทำ/รอออกแบบ |
| `tenant:line-account:company-upsert-missing` | สูง | 9/8/2569 | 2 fallback writes | ผูก LINE สำรองล้มและสิทธิ์หลายบริษัทไม่ชัด | LINE/Auth | 4 ชม.หลังเริ่มแก้ | พร้อมทำ/ยังไม่ Deploy |
| `tenant:regression:coverage-gap` | สูง | 9/8/2569 | 12 module groups | Build/Lint ผ่านแต่ไม่ตรวจ runtime tenant behavior | QA/ทุกโมดูล | ก่อน Deploy ชุดแก้ | พร้อมทำ/ยังไม่ Deploy |
| `attendance-reminder:monitor-secret-missing` | วิกฤต | 9/8/2569 | 1 Production smoke test | การปิด Gateway JWT เดิมเปิดให้เรียกได้เพราะ Secret ไม่มี; หลังแก้ fail-closed Automation ตอบ 503 จนกว่าจะตั้ง Secret | Automation/Security | 1 ชม. | ปิดช่องแล้ว/รอตั้ง Secret และ Cron |

### ข้อมูลขั้นต่ำต่อ Error

- `error_reference`, `tenant/company`, module, page/route และ environment
- fingerprint, error code, sanitized message/stack และเวลาครั้งแรก/ล่าสุด
- จำนวนครั้ง ผู้ใช้/อุปกรณ์/โครงการที่ได้รับผลกระทบ โดยไม่บันทึก Password, Token หรือ Secret
- severity, priority, SLA, ผู้รับผิดชอบ และสถานะงาน
- สาเหตุที่คาดการณ์ หลักฐาน ความมั่นใจ แนวทางแก้และความเสี่ยง
- ผล Build/Lint/Test, deployment, smoke test, ผู้อนุมัติ และ regression count

### หลักป้องกันระบบ

- หากส่ง Error เข้าฐานข้อมูลไม่ได้ ให้พักข้อมูลแบบจำกัดจำนวนในเครื่องและส่งซ้ำเมื่อ Online
- จำกัดจำนวน Event และรวมเหตุการณ์ซ้ำเพื่อไม่ให้ระบบหนักหรือแจ้ง LINE รัว
- ตัดข้อมูลส่วนบุคคลและ Secret ก่อนบันทึกทุกครั้ง
- AI เสนอแนวทางได้ แต่การแก้ Migration, สิทธิ์, Secret, ลบข้อมูล และงานย้อนกลับยากต้องได้รับอนุมัติ

## เสร็จในโค้ดและผ่านการตรวจ

- หน้า `สถานะระบบ` แบบกราฟิก: สถานะรวม, uptime, response time, เวลาตรวจล่าสุด/ครั้งถัดไป
- แผนภาพเส้นทางระบบ พร้อมสถานะแต่ละจุดและข้อความผิดพลาดล่าสุด
- กราฟแนวโน้ม 24 รอบ และจำนวนปกติ/เฝ้าระวัง/ขัดข้อง/ยังไม่ตรวจ
- ตารางรายละเอียดระบบ เหตุขัดข้อง และประวัติแจ้ง LINE
- ตั้งค่ารอบตรวจ 5/15/30/60 นาที, จำนวนรอบก่อนเตือน, เตือนซ้ำ และเวลาสรุปรายวัน
- แก้ข้อความภาษาไทยเพี้ยนในหน้า Status
- Production build และ ESLint ผ่าน

## งานที่ต้องทำต่อก่อนถือว่าระบบตรวจสอบสมบูรณ์

1. Scheduled Monitor 24 ชั่วโมง
   - ปัจจุบันตัวตรวจอัตโนมัติทำงานเมื่อ Admin เปิดหน้าเว็บ
   - ต้องเพิ่มตัวเรียกเบื้องหลังที่ยืนยันตัวตนได้ โดยไม่เปิดเผย service key

2. LINE heartbeat
   - บันทึกเวลาที่ Webhook รับ Event สำเร็จล่าสุด
   - บันทึกเวลาตอบกลับ LINE สำเร็จล่าสุด
   - แจ้งเตือนกรณีไม่มี Event เข้าในช่วงเวลาที่กำหนด
   - แยกสถานะ Token, Webhook, วิเคราะห์ข้อความ, บันทึกฐานข้อมูล และตอบกลับ

3. ตรวจหน้า Status ด้วยบัญชี Admin
   - ตรวจ desktop และมือถือจริง
   - ตรวจปุ่ม “ตรวจทันที”, บันทึก Config, ตาราง และ Export
   - การทดสอบแบบไม่เข้าสู่ระบบผ่านแล้วถึงหน้า Login แต่ข้อมูล Status ต้องใช้สิทธิ์ Admin

4. Production handoff
   - ตรวจ migration ที่ Production
   - deploy frontend และ Edge Function เวอร์ชันเดียวกัน
   - smoke test ระบบลงเวลา, LINE webhook, approval และ health alert

## เกณฑ์ปิดงาน

- Build และ Lint ผ่าน
- ตรวจอัตโนมัติทำงานแม้ไม่มี Admin เปิดเว็บ
- LINE เงียบต้องถูกตรวจพบ ไม่ใช่ตรวจเฉพาะรายการที่ล้มเหลวหลังรับเข้าแล้ว
- แจ้งเตือนครั้งแรกและเตือนซ้ำไม่ส่งถี่เกินค่า Config
- ข้อมูลแยกตามบริษัทและสิทธิ์ผู้ใช้งาน
- มีหลักฐานทดสอบ Production ของ desktop และมือถือ

## TEN-002 update — 9/8/2569

## SYS-009 — Single source of truth สำหรับรายการงาน — 9/8/2569

> ยืนยัน Production 9/8/2569: ผู้ใช้อนุมัติ Migration SYS-009 และ Deploy หน้าเว็บ/health-monitor แล้ว สถานะ `กำลังทำ`; ลำดับดำเนินการคือ Migration → ตรวจข้อมูล/RLS → Edge Function → Frontend → smoke test

> ผล Production 9/8/2569: Migration `202608090012` สำเร็จ; ฐานข้อมูลมี 23 งานและ audit เริ่มต้น 23 รายการ; RLS สำหรับอ่าน/อัปเดต/ประวัติทำงาน; `health-monitor` Deploy สำเร็จ; Vercel deployment `dpl_J41pffCUMNajmsErwiPz53ebVuNy` READY และ alias `wisdomai-react.vercel.app`; bundle `SystemHealth-DDGNTERw.js` อ่าน `system_work_items` และมีแท็บหลายบริษัท; หน้า unauthenticated redirect `/login` โดยไม่มี console error. SYS-009 ในฐานข้อมูลกลางเป็น `review` 95% / `deployed`; รอ Admin UAT ยืนยันว่าเห็น 23 งานและ LINE report ใช้รายการเดียวกัน

> หลักฐานตรวจซ้ำ 10/8/2569: ตรวจ Source ปัจจุบันยืนยันว่า Web (`SystemHealth`), รายงาน/คำขออนุมัติ LINE (`health-monitor` และ `line-webhook`) และ Automation (`automation-worker`) อ่านหรืออัปเดต `system_work_items`; การเปลี่ยนสถานะ/ความคืบหน้าถูกบันทึกผ่าน trigger ไป `system_work_item_events`. `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run test:auth-routing` และ `npm.cmd run test:line-quota` ผ่านทั้งหมด. ไม่ได้เปลี่ยน schema/RLS, รัน Migration หรือ Deploy; SYS-009 คง `review` 95% / `deployed` จนกว่า Admin UAT จะยืนยันข้อมูล 23 งานบน Web และรายงาน LINE จากรายการเดียวกัน

> หลักฐาน Automation รอบล่าสุด 10/8/2569: ตรวจซ้ำจาก working tree เดิมพบว่า Web (`SystemHealth`), LINE (`health-monitor`, `line-webhook`), Telegram admin และ Automation (`automation-worker`) ใช้ `system_work_items` เดียวกัน; audit ยังคงผ่าน `system_work_item_events`. `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run test:auth-routing` และ `npm.cmd run test:line-quota` ผ่านทั้งหมด (build สร้าง `SystemHealth-d21W3CGH.js`). ไม่มีการแก้ schema/RLS, รัน Migration หรือ Deploy. คง SYS-009 ที่ `review` 95% / `deployed`; blocker ที่เหลือคือ Admin UAT ยืนยัน 23 งานบน Web และรายการเดียวกันผ่าน LINE

> หลักฐานตรวจรอบ SYS-009 ล่าสุด 10/8/2569: ยืนยัน Source อีกครั้งว่า Web (`SystemHealth`), LINE (`health-monitor`, `line-webhook`), Telegram admin และ Automation (`automation-worker`) อ้างอิง `system_work_items`; trigger ใน Migration `202608090012` บันทึกการเปลี่ยนแปลงไป `system_work_item_events`. ตรวจรับ working tree ปัจจุบันด้วย `npm.cmd run lint`, `npm.cmd run build`, `npm.cmd run test:auth-routing` และ `npm.cmd run test:line-quota` ผ่านทั้งหมด (build สร้าง `SystemHealth-D16KoOvY.js`). รอบนี้อัปเดตเฉพาะหลักฐานของ SYS-009 รายการเดิม ไม่แก้ source/schema/RLS, ไม่รัน Migration และไม่ Deploy. คง SYS-009 ที่ `review` 95% / `deployed`; blocker fingerprint `sys009.admin_uat.pending` คือ Admin UAT ยืนยัน 23 งานบน Web และรายงาน LINE จากรายการเดียวกัน

> อัปเดต: สร้าง Migration `202608090012_system_work_items_source_of_truth.sql`, ตารางงานกลางและ audit events พร้อม RLS; หน้า System Health และรายงาน LINE เปลี่ยนมาอ่าน `system_work_items` แล้ว รองรับ 23 งาน SYS/OPS/AUD/TEN. Deno check, lint และ build ผ่าน. สถานะ `รอยืนยัน` Migration Production เนื่องจากเป็น schema/RLS ใหม่; Frontend และ Edge ยังไม่ Deploy เพื่อไม่ให้หน้าเว็บเรียกตารางที่ยังไม่มี

## SYS-010 — ทำงานรายการค้างต่อเนื่องอัตโนมัติ — 9/8/2569

> ขอบเขต: ปรับ Automation `wisdomai` เดิมจากรอบ 2 ชั่วโมงเป็นรอบถี่แบบไม่สร้าง Automation ซ้ำ อ่านคิวหลักจาก `public.system_work_items` เลือกงาน `พร้อมทำ/กำลังทำ` ตามความสำคัญ ทำงานที่ปลอดภัยต่อเนื่อง อัปเดตสถานะและหลักฐานกลับแหล่งเดียวกัน และหยุดเฉพาะรายการที่ต้องอนุมัติด้าน Migration, Secret, สิทธิ์, การลบข้อมูล หรือการเปลี่ยนแปลงย้อนกลับยาก
>
> สถานะ: `รอตรวจ` | ความเสี่ยง: ปานกลาง (ต้องป้องกันงานซ้อนและ Deploy ชนกัน) | หลักฐานทดสอบ: อัปเดต Automation `wisdomai` สำเร็จ; ตรวจไฟล์ตั้งค่าจริงพบสถานะ ACTIVE, รอบทุก 15 นาที, prompt อ่าน `system_work_items` และนโยบายแจ้งเฉพาะรอบล้มเหลว; upsert SYS-010 ลงฐานข้อมูลกลางสำเร็จ | วันที่อัปเดต: 9/8/2569 | Production status: `ใช้งานแล้ว`

> ขอบเขตเพิ่มที่ยืนยัน 9/8/2569: งานที่ต้องตัดสินใจต้องส่งคำขออนุมัติไปยัง LINE กลุ่ม “กลุ่มทดสอบโปรแกรม” เป็นช่องทางสำรองจากหน้าเว็บ โดยแสดงเลขงาน ขอบเขต ความเสี่ยง และปุ่มอนุมัติ/ไม่อนุมัติ; รับผลเฉพาะบัญชี LINE ที่ผูกกับผู้ดูแลระบบ และบันทึกผลกลับ `system_work_items` พร้อม audit. Deploy `health-monitor` และ `line-webhook` สำเร็จบน Production `xkieyqixlufjqructjkr`; smoke test แบบไม่มีสิทธิ์ตอบ HTTP 401 ทั้งคู่ตามคาด; lint/build ผ่าน; Automation อัปเดตให้เรียก `send_work_approval` แล้ว และ SYS-010 ในฐานข้อมูลกลางเป็น `review` 98% / `deployed`. สถานะ `รอตรวจ`; หลักฐานที่เหลือ: UAT ส่งคำขอจริงและกดอนุมัติจาก LINE กลุ่มทดสอบโปรแกรม

> แก้กติกา 9/8/2569: พบ Automation หยุดทั้งคิวเมื่อเจองานแรกที่ต้องอนุมัติ ทำให้รายการพร้อมทำอื่นไม่เดิน แก้ให้พักเฉพาะงานเสี่ยงเป็น `review` แล้วเลือกงานปลอดภัยลำดับถัดไปต่อทันที แม้ LINE ส่งอนุมัติไม่ได้; เริ่ม SYS-002 เป็นงานถัดไป. สถานะ `กำลังทำ`; Production status: `กำลังปรับ Automation`

> ขอบเขตเพิ่มที่ยืนยัน 9/8/2569: เพิ่มการอนุมัติงานระบบสถานะ `review` บนหน้า Web `/system-health` เป็นช่องทางหลัก ควบคู่ LINE โดยอนุมัติเป็น `ready`, ไม่อนุมัติเป็น `blocked`, ต้องบันทึกเหตุผล ผู้ดำเนินการ เวลา และ audit. ทำเสร็จและ Deploy Vercel Production `7eVogpyxCsuDnt3tZRrmxGwZR3yU` พร้อม alias แล้ว; lint/build ผ่าน; SYS-010 กลางเป็น `review` 99% / `deployed`. สถานะ `รอตรวจ`; หลักฐานที่เหลือ: Admin UAT กดตัดสินใจหนึ่งรายการ

- ขอบเขต: ให้ฐานข้อมูลเป็นแหล่งรายการงานกลางเพียงจุดเดียว; หน้า System Health, รายงาน LINE, Automation และ Dashboard อ่าน/อัปเดตสถานะเดียวกัน พร้อม tenant/สิทธิ์/audit
- สถานะ: `กำลังทำ` (ผู้ใช้ยืนยัน 9/8/2569)
- ความเสี่ยง: สูง — ต้องเพิ่ม schema/RLS และย้ายรายการ SYS/AUD/TEN เดิมโดยไม่สร้างรายการซ้ำ
- เกณฑ์ตรวจ: migration ผ่าน, หน้าเว็บอ่านข้อมูลจริง, LINE สรุปจากข้อมูลเดียวกัน, build/lint/Deno ผ่าน, smoke test สิทธิ์และ Production
- Production status: ยังไม่ Deploy

> ยืนยันเพิ่มเติม 9/8/2569: ผู้ใช้อนุมัติให้นำ Migration `secure attendance reminder Cron` ขึ้น Production แล้ว สถานะเปลี่ยนเป็น `กำลังทำ`; ความเสี่ยงคือ Security Definer/Vault/Cron และต้องตรวจสิทธิ์กับรอบเรียกจริงหลัง Deploy

> ผล Production 9/8/2569: Migration `202608090011` สำเร็จ; Cron job id 4 เปิดใช้งานทุก 5 นาที, Secret ใน Vault ยาว 64 ตัวอักษร, helper เป็น `SECURITY DEFINER` และ anon/authenticated ไม่มี execute permission; smoke request id 1777 ตอบ HTTP 200 (`companies=1`, `checked=4`, `events=[]`). สถานะ TEN-002 เปลี่ยนเป็น `รอตรวจ` สำหรับ UAT รอบเวลา 20:00/00:00/07:00; Production status = Deploy แล้ว. Dashboard ล่าสุด: พร้อมทำ 14, กำลังทำ 0, รอตรวจ 6, เสร็จแล้ว 2, ติดปัญหา 0; Error ใหม่ 0, Error ซ้ำ 0, SLA เกินกำหนด 0.

- สถานะ: `ติดปัญหา` เฉพาะขั้นนำ Migration Cron ขึ้น Production; Edge Function และ Secret พร้อมแล้ว
- ขอบเขตที่ทำแล้ว: Cron ใช้ `ATTENDANCE_MONITOR_SECRET`; การตรวจด้วยมือใช้ User JWT และตรวจสมาชิกบริษัทระดับ company_admin/executive/manager ภายใน Function
- หลักฐาน: Deno check ผ่าน, `npm.cmd run lint` ผ่าน, `npm.cmd run build` ผ่าน, Edge Function deploy สำเร็จ และ Production smoke test แบบไม่มีสิทธิ์ตอบ HTTP 401
- Security: หมุน Secret ด้วย CSPRNG แล้ว เก็บชื่อ `ATTENDANCE_MONITOR_SECRET` ใน Edge Secrets และสำเนาเข้ารหัสชื่อ `attendance_monitor_secret` ใน Supabase Vault; ไม่มีค่า Secret ในเอกสารหรือ source control
- Migration: `202608090011_secure_attendance_reminder_cron.sql` พร้อมใช้ Security Definer ที่ revoke สิทธิ์ public/anon/authenticated และ Cron เรียกผ่าน Vault
- ความเสี่ยง: Migration สร้าง Security Definer, อ่าน Vault และเปลี่ยน Cron ถาวร จึงรอคำยืนยันเฉพาะรายการก่อน `db push`
- Production status: Edge ACTIVE; Secret/Vault ตั้งแล้ว; Cron รุ่นปลอดภัยยังไม่ Deploy
- Dashboard ล่าสุด: พร้อมทำ 14, กำลังทำ 0, รอตรวจ 5, เสร็จแล้ว 2, ติดปัญหา 1; Error ใหม่ 0, Error ซ้ำ 1, SLA เกินกำหนด 1
## SYS-012 — รวมหน้าตรวจสอบ LINE และ Telegram — 9/8/2569

> ขอบเขต: เพิ่มแท็บ `LINE` และ `Telegram` ในหน้า `/line-monitor` เดิม โดย Telegram ต้องแสดงเหตุการณ์ข้อความ/เสียง สถานะประมวลผล คำสั่งหรือข้อความที่ถอดได้ เวลา และ Error ตามสิทธิ์บริษัทของ Admin/Manager
>
> สถานะ: `รอยืนยัน` | ความเสี่ยง: ปานกลางเฉพาะการ Deploy `telegram-admin` เพราะ Telegram Webhook ต้องเปิด endpoint โดย `verify_jwt=false` และตรวจความถูกต้องภายใน Function | หลักฐานทดสอบ: `npm.cmd run lint` ผ่าน; `npm.cmd run build` ผ่าน; Production deployment `dpl_4fyjcXTzTC4tuXspWGAVB4YmFnZG` READY; alias `wisdomai-react.vercel.app`; smoke testหน้า root สำเร็จ; พบและแก้ Edge Function ให้บันทึกข้อความถอดเสียงลง `command`; การ Deploy Edge ถูกหยุดเพื่อรออนุมัติด้าน security setting | วันที่อัปเดต: 9/8/2569 | Production status: `Frontend Deploy แล้ว/telegram-admin ยังไม่ Deploy`

## SYS-004 Incident — ซ่อม Local Automation Runner ให้รองรับ Codex CLI ปัจจุบัน — 10/8/2569

> ขอบเขต: แก้ runner ที่เรียก `codex exec` ด้วย `--sandbox workspace-write` พร้อม `--approve-for-me` ซึ่ง CLI รุ่นปัจจุบันไม่อนุญาตให้ใช้ร่วมกัน ทำให้คิวถูก claim แล้วล้มเหลวก่อนเริ่มงาน; คง automatic review และ workspace-write โดยใช้ `--approve-for-me` เพียงตัวเดียว
>
> สถานะ: `เสร็จแล้ว` | ความเสี่ยง: ต่ำ | Fingerprint 1: `local_runner.codex_cli.sandbox_approve_conflict`; Fingerprint 2: `local_runner.codex_cli.nonzero_with_valid_result` | ผลกระทบ: SYS-008 รอบ 00:44 ไม่เริ่มประมวลผล และ SYS-009 รอบ 00:47 ทำงานเสร็จแต่ถูกตีสถานะผิดเป็น blocked | การแก้ไข: ใช้ `--approve-for-me` โดยไม่ส่ง `--sandbox` ซ้ำ, ถือ structured result ที่ parse ได้เป็นผลหลักแม้ process exit code ไม่เป็นศูนย์ และเพิ่ม worker action จำกัดขอบเขตสำหรับคืนเฉพาะรายการ `blocked/local_runner_failed` เข้าคิว | หลักฐาน: PowerShell parser ผ่าน, lint/build ผ่าน, SYS-009 ทำ lint/build/auth-routing/line-quota ผ่านและสร้าง JSON ตาม schema สำเร็จ; `automation-worker` Deploy Production สำเร็จ; recovery action คืน SYS-009 เป็น ready สำเร็จ | วันที่อัปเดต: 10/8/2569 | Production status: `local runner patched / automation-worker deployed / recovery smoke passed`

## SYS-004 Incident — approval-loop queue starvation — 10/8/2569

## SYS-004 Workforce readiness tenant-view follow-up — 11/8/2569

> Fingerprint: `health_monitor.employee_readiness.company_id_missing`. Migration `202608110023` repairs `employee_onboarding_readiness` as a company-scoped, security-invoker view. Health Monitor now serializes structured Supabase errors, and System Health shows cause, impact, remediation, confidence, and technical detail instead of `[object Object]`. Production migration, Edge Function, and Vercel deployment passed tests, lint, build, and smoke tests. Status remains review 95% until the next authenticated or scheduled health run confirms Workforce is no longer critical.

## SYS-004 Incident — authoritative queue completion drift — 10/8/2569

> Follow-up TEN-006: พบแถวคิวเดิมยังเป็น ready/awaiting_approval แม้ Migration 202608100005, Frontend Production และ smoke test ผ่านแล้ว; Migration 202608100008 แบบ guarded update ขึ้น Production สำเร็จและ local/remote ตรงกัน; TEN-006 เป็น done 100%, current_step=completed, ไม่มี active worker run | สถานะ: `เสร็จแล้ว` | Production status: `migration_applied_verified`

> ขอบเขต: ซิงก์ TEN-002, TEN-004, TEN-005 และ TEN-008 ที่มีหลักฐาน Production ครบกลับเป็น done; คืน TEN-001 เป็น review/deployed_pending_uat; ทุก update มี guard ready + awaiting_approval + worker_id is null และใช้ trigger เดิมบันทึก Audit
>
> สถานะ: `เสร็จแล้ว` | ความเสี่ยง: สูง | Fingerprint: `automation.queue.completion_state_drift` | ผลกระทบเดิม: Dashboard แสดงงานเสร็จแล้วเป็นงานพร้อมทำและเสี่ยงอนุมัติซ้ำ | หลักฐาน: Migration `202608100007` ขึ้น Production และ local/remote ตรงกัน; TEN-002/004/005/008 เป็น done 100%; TEN-001 กลับเป็น review 90% รอ UAT; คิวล่าสุด ready 9, review 2, done 14, active runs 0; regression test, lint และ build ผ่าน | Production status: `migration_applied_verified`

> Source verification 11/8/2569 (100% work-item recheck): focused regression coverage passed for both sides of SYS-004—the atomic worker claim skips `production_status=awaiting_approval`, and the health monitor aggregates non-healthy checks into the existing `SYS-004` row with sorted module/check fingerprints. `npm.cmd run test:automation-claim`, `npm.cmd run test:error-fingerprint`, `npm.cmd run lint`, and `npm.cmd run build` all completed successfully; lint reported one unrelated existing `react-hooks/exhaustive-deps` warning in `src/pages/Reports/index.tsx` and no errors. No migration, security change, deployment, or production mutation was run in this verification.

> ขอบเขต: ป้องกันงาน `production_status=awaiting_approval` ถูก Worker claim ซ้ำก่อนผู้ดูแลตัดสินใจ และทำให้งานปลอดภัยด้านหลังไม่ได้เริ่ม; ช่องทางอนุมัติ Web, LINE และ Telegram ต้องบันทึก `approved_for_execution` หรือ `rejected_by_admin` ให้ชัดเจน
>
> สถานะ: `เสร็จแล้ว` | ความเสี่ยง: สูง (Database claim migration) | Fingerprint: `automation.claim.awaiting_approval_starvation` | ผลกระทบ: คิวพร้อมทำวนอยู่ที่งานความเสี่ยงสูงรายการเดิม | หลักฐานทดสอบ: Migration `202608090021` เป็นรายการค้างเพียงไฟล์เดียวและ deploy สำเร็จ; lint/build ผ่าน; LINE/Telegram Edge Functions deploy สำเร็จ; Vercel Production deploy และ alias สำเร็จ; smoke test พบ Worker ข้ามงาน `awaiting_approval` และ claim `SYS-009` เป็น `doing` พร้อม active run 1 รายการ | วันที่อัปเดต: 10/8/2569 | Production status: `deployed / approval gate smoke passed`
> Production repair 11/8/2569: ผู้ใช้อนุมัติ Migration `202608110027_health_monitor_company_bootstrap.sql`; เติม `health_monitor_settings` ที่ขาดให้ TEC โดยไม่คัดลอก LINE group จากบริษัทอื่น และขยาย `seed_company_singleton_settings()` ให้บริษัทใหม่มี Health Monitor settings อัตโนมัติ. หน้า System Health ใช้ `maybeSingle()` เพื่อไม่แสดง PostgREST single-row coercion error ระหว่าง bootstrap; `health-monitor` ส่งข้อความ 409 ที่ชัดเจนหาก setting ยังไม่พร้อม. Tenant/scheduler tests, lint (0 errors; warning เดิม 1 จุด), build, Supabase Function deploy และ Vercel Production deploy ผ่าน. Smoke test: บริษัท active 2, company settings 2, missing 0, TEC setting present และ LINE group ว่าง, Web HTTP 200, Function ไม่มีสิทธิ์ HTTP 401 ตามคาด.
> TEN-011 Production update 11/8/2569: ผู้ใช้อนุมัติ Migration `202608110028_platform_line_group_company_mapping.sql` และหน้า LINE Monitor สำหรับ Platform Admin. เพิ่ม RPC ดู/ย้ายเจ้าของกลุ่ม LINE แบบ Security Definer ที่ตรวจ `is_platform_admin()` ภายในฐานข้อมูล; ก่อนย้ายยกเลิกปลายทาง Health Monitor, Workforce และ Project Site ของบริษัทเดิม, ล้าง project mapping, เก็บประวัติข้อความเดิมไว้ tenant เดิม และบันทึก `line_group_company_assigned` audit. หน้าเว็บเพิ่มแท็บ “จัดการกลุ่ม LINE” เฉพาะ Platform Admin พร้อมยืนยันก่อนย้าย. Mapping/TEN-011 tests, lint (0 errors; warning เดิม 1), build ผ่าน; Migration local/remote ตรงกัน, Vercel Production HTTP 200 และ RPC ไม่มีสิทธิ์ HTTP 401. รอ Platform Admin เลือกกลุ่มทดลองและยืนยัน UAT.
> TEN-011 UI repair 11/8/2569: แก้ dropdown จัดการกลุ่ม LINE ที่แสดงรายการบริษัทว่าง เพราะหน้าอ่าน `name` แต่ RPC `get_my_companies()` ส่ง `company_name`; ปรับ type/render ให้ใช้ contract จริงและแสดงบริษัทที่กำลังใช้งาน. Mapping test, lint 0 errors (warning เดิม 1) และ build ผ่าน; Deploy Vercel Production สำเร็จ. กลุ่ม “กลุ่มทดสอบโปรแกรม” คงอยู่ WisdomAI Construction สำหรับ Platform/Test และไม่ย้ายไป TEC; รอ UAT ย้ายเฉพาะกลุ่มงานของ TEC.
> TEN-011 audit repair 11/8/2569: Production UAT พบ `app_activity_logs_event_type_check` ปฏิเสธ event `line_group_company_assigned`; transaction rollback จึงไม่มีการย้ายค้างบางส่วน. เพิ่ม Migration `202608110029_fix_line_group_mapping_audit.sql` เพื่ออนุญาต event เฉพาะนี้และคง Platform Admin bootstrap flag จนบันทึก audit เสร็จแล้วปิดทันที. Mapping/TEN-011 tests, lint 0 errors (warning เดิม 1), build ผ่าน และ Migration local/remote ตรงกัน. รอผู้ใช้กดบันทึกการผูกกลุ่มเดิมซ้ำเพื่อ UAT.
> TEN-011 UAT complete 11/8/2569: กดบันทึกการผูก “ทีมบ่าว” → TEC ซ้ำสำเร็จหลัง audit repair; Production บันทึก `line_group_company_assigned` รวม 2 ครั้ง ครั้งล่าสุด 09:16 น. ICT และชี้ไป TEC ถูกต้อง. ปิดงานเป็น done 100%.
> LINE-GROUP-APPROVAL-001 backlog 11/8/2569: เมื่อ Bot พบกลุ่ม LINE ใหม่ ให้กักข้อมูลเป็น `unassigned` แล้วแจ้ง Telegram Admin เป็นช่องทางหลักและ LINE Admin เป็น fallback เพื่อให้ Platform Admin เลือกบริษัทและยืนยันการผูก; ต้อง deduplicate การแจ้ง, บันทึก audit ผู้อนุมัติ/เวลา และห้าม route ข้อมูลเข้า tenant ก่อนผูกสำเร็จ. บันทึกเป็น review 0% เพราะต้องอนุมัติ Migration/RPC และสิทธิ์ Platform Admin ก่อนดำเนินการ.
> EMP-LINE-ONBOARD-001 review 11/8/2569: ตรวจพบเอกสาร LINE ชุดเดียวกัน 3 รูป (ทะเบียนบ้าน บัตรประชาชน และระเบียนการศึกษา) จากต้นฉบับเดิมและการส่งซ้ำในกลุ่มทดสอบโปรแกรม; ไฟล์เก็บสำเร็จแต่ถูกข้ามวิเคราะห์เพราะ duplicate hash. Schema ปัจจุบันบังคับ `profile_id` จึงยังสร้าง candidate ที่ไม่มี Auth account ไม่ได้. ขอ Migration สร้าง tenant-scoped employee intake/draft, ผูกเอกสารเดิมโดยไม่ทำสำเนา, จำกัดข้อมูลสกัด, ตรวจชื่อที่อ่านไม่ตรงกัน และให้ Admin ยืนยันก่อนสร้าง Profile/Employment/สิทธิ์. ไม่บันทึกเลขบัตรหรือข้อมูลเอกสารใน work evidence.
> EMP-LINE-ONBOARD-001 channel update 11/8/2569: ขยาย Wizaz Intake ให้ใช้ workflow กลางเดียวกันบน Telegram, LINE และ Web Chat; เริ่ม Telegram UAT ก่อน. Web Chat ต้องรองรับส่งรูป/ไฟล์ ดูตัวอย่าง ตอบข้อมูลที่ขาด แก้ไขร่าง ส่งตรวจ และอนุมัติตามสิทธิ์ โดยห้ามเก็บข้อมูลอ่อนไหวในข้อความหรือ log.
## EMP-LINE-ONBOARD-001 — Telegram employee intake (2026-08-11)

> Conversation continuity update 11/8/2569: Telegram Admin สามารถตอบชื่อและข้อมูลที่ขาดต่อเนื่องในห้องเดิมได้โดยไม่ต้อง Reply เมื่อมี intake เปิดอยู่เพียงรายการเดียวใน tenant; รองรับอักขระสระ/วรรณยุกต์ภาษาไทย, ไม่เดาเมื่อมีหลายร่าง, และ Production UAT รับชื่อจากข้อความธรรมชาติสำเร็จโดยไม่บันทึก PII ใน work evidence.
> Duplicate delivery update 11/8/2569: เพิ่ม fingerprint สำหรับข้อความข้อมูลพนักงานขาเข้า 15 นาที, รองรับรายการ `pending_review` เพื่อแยกการแก้ไขกับข้อความซ้ำ และไม่ส่งสรุป Telegram ซ้ำเมื่อ update ใหม่มีเนื้อหาเดิม.

- Production migration `202608110030_employee_multichannel_intake.sql` applied.
- `telegram-admin` deployed with webhook-secret validation unchanged.
- Telegram flow: receive document photo, ask purpose, extract allowlisted employee fields, request missing fields, then hold for Admin review.
- Tenant isolation: company-scoped rows, RLS, and private company-path storage policies.
- Privacy: no full national ID, laser code, religion, raw OCR, or household-member data is retained in the intake summary.
- Verification: employee-intake tests, Telegram attendance regression tests, lint (0 errors), build, and Production smoke tests passed. Telegram UAT is pending.
- Source recheck 2026-08-11: `npm.cmd run test:employee-intake` and `npm.cmd run build` passed; `npm.cmd run lint` completed with 0 errors and the existing unrelated `react-hooks/exhaustive-deps` warning in `src/pages/Reports/index.tsx`.
- Remaining scope is held in `review`: the shared schema names Telegram, LINE, and Web Chat and the Telegram service exposes the LINE import bridge, but no Web Chat upload/preview/missing-data/draft-edit/review UI exists yet, and there is no approved intake finalization contract that creates Employment/Auth/rights. Completing that contract requires reviewed schema/RPC/security work, so no migration, deployment, permission change, secret operation, or Production mutation was performed. Fingerprint: `employee.intake.web_approval_contract_missing`.
- Safe revalidation 2026-08-11 (EMP-LINE-ONBOARD-001 at 99%): `npm.cmd run test:employee-intake` and `npm.cmd run build` passed; `npm.cmd run lint` passed with 0 errors and the existing unrelated `react-hooks/exhaustive-deps` warning in `src/pages/Reports/index.tsx`. Status remains `review` because the missing Web Chat and approval/finalization scope crosses schema/RPC/security boundaries; no migration, deploy, permission change, secret operation, data mutation, or Production action was performed. Fingerprint remains `employee.intake.web_approval_contract_missing`.
- Approval-action preparation 2026-08-11: Telegram callback messages are now finalized in place after an Admin action, their inline buttons are removed, and the message records the selected action, actor, Bangkok time, and resulting status. Attendance and work-item approval callbacks use the same behavior. A new idempotent employee-intake approval path and migration `202608110031_employee_people_without_login.sql` are prepared: it creates a tenant-scoped Employee Master independent from Auth/Login, links exactly one employee to one intake, and grants no application permission. Employee-intake regression test, lint (0 errors; the existing unrelated Reports warning remains), and build passed. Production migration and `telegram-admin` deployment are intentionally paused for explicit approval.
- Production completion 2026-08-11: migration `202608110031_employee_people_without_login.sql` and `telegram-admin` were deployed after explicit approval. The approved intake created one tenant-scoped Employee Master in `preboarding`; the intake is `approved`, no Auth/Login profile was created, and replaying approval returned `already_created` with the employee count remaining one. Thai employment labels are normalized to the database codes before approval. Production secret-guard smoke returned HTTP 401 without credentials. Employee-intake test, lint (0 errors; one pre-existing Reports warning), and build passed.
> PAYROLL-FORECAST-001 (11/8/2569): ระบบค่าแรง Real-time และประมาณการสิ้นเดือนขึ้น Production แล้ว เพิ่มประวัติอัตราค่าจ้างแบบมีผลตามวันที่, แผนกำลังคนรายวัน, RPC แยกบริษัทสำหรับยอดเกิดขึ้นจริง/รวมแผน/ประมาณการ และหน้า Reports แสดงข้อมูลขาด โดยไม่แก้ Payroll/Payslip หรืองวดที่ล็อกแล้ว. Migration `202608110035_realtime_payroll_forecast.sql` ขึ้น Production สำเร็จ; REST smoke พบตารางใหม่ HTTP 200 และ RPC ปฏิเสธ anon HTTP 401 ตามคาด; `test:payroll-forecast`, lint และ build ผ่าน (lint มี warning เดิม 1 รายการใน Reports). Vercel deployment `28A65eQmd1mMKmPnMPEMqtsm6K7v` READY และ alias `wisdomai-react.vercel.app`; `/reports` HTTP 200 และ lazy bundle `Reports-BCGeRIMP.js` มี `get_realtime_payroll_forecast`. คงสถานะ review จน Admin UAT ตรวจยอดจริงในระบบที่ล็อกอิน.

> PAYROLL-FORECAST-001 UI/PDF update (12/8/2569): Reports now shows daily and monthly employees, net payable days, late/early-leave/after-shift day counts and minutes, approved OT, current accrued wage/salary, and source/status. Daily labor uses 0/0.5/1-day rules from valid worked time; monthly labor is accrued only through the current scheduled workday, while full-month value remains forecast-only. Employee detail contains a complete daily wage breakdown and an individual A4 landscape print/save-PDF action. Tenant context remains company-scoped. Verification passed: lint 0 errors (one existing Reports hook warning), build, `realtime-payroll-forecast.test.ts`, and `workforce-calculation.test.ts`. Production deployment `EDntTQswLCBodyZcTfC2mZzPWECe` is aliased to `wisdomai-react.vercel.app`; `/reports`, main bundle, and `Reports-B1iTZipn.js` returned HTTP 200, with all four UI markers confirmed. Status remains `review` pending signed-in Admin UAT of real employee totals and personal PDF output.

> PAYROLL-FORECAST-001 zero/status/PDF-name update (12/8/2569): Display-only zero values without activity are rendered as `-`; calculations retain numeric zero. A scheduled past date without attendance is labeled `หยุด`, while the current date is `ยังไม่ลงเวลา`; approved leave, company holiday, scheduled holiday, exempt policy, and incomplete attendance retain distinct states. The individual print/PDF document now states `ชื่อพนักงาน`, employee identifier, month, and employee name in the document title so the saved PDF is attributable. Lint passed with 0 errors (one pre-existing hook warning), build and payroll/workforce regression tests passed. Vercel deployment `7gBjv4riVGSMkhRs2qJR3Ub2V1Q3` is aliased to Production; `/reports` and `Reports-CVIG76xd.js` returned HTTP 200 and all required markers were present. Status remains `review` for signed-in visual/PDF UAT.

> HOLIDAY-WAGE-001 (12/8/2569): Migration `202608120007_holiday_wage_override_audit.sql` is applied to Production and remote migration history confirms `202608120007`. Daily and monthly holiday work now defaults to 1x without dropping the worked day, remains visibly pending until Admin confirms, and supports audited 1/1.5/2/3x overrides plus separately approved holiday OT. Coverage includes global company holidays, site-specific holidays, and weekly/scheduled holidays; locked pay periods reject changes. Tenant access is company-scoped through manager checks and RLS. `test:holiday-wage`, lint, local production build, Vercel production build, `/reports`, and the resolved `Reports-CPjuE8wE.js` asset all passed; all five Production markers were confirmed. Vercel deployment `HHtDhF3iDPAh3Ka2PsmUDbN1VE84` is aliased to `wisdomai-react.vercel.app`. Status: deployed; signed-in UAT is still recommended before payroll close.

> EFFECTIVE-WORKDAY-001 prepared (13/8/2569): จัดทำมาตรฐานคำนวณวันทำงานกลาง แยกเวลาเข้า–ออกจริงซึ่งเป็นหลักฐานออกจากช่วงเวลาที่มีผลต่อการคำนวณ โดยรองรับเต็มวัน ครึ่งวันเช้า ครึ่งวันบ่าย ช่วงกำหนดเอง และปรับค่าแรงอย่างเดียว ตัวชี้วัดสาย/ออกก่อน/ออกเกินกะใน Reports ใช้ขอบเขตที่มีผล จึงไม่เอาครึ่งวันไปเทียบกับกะเต็มวันอีก Migration `202608130001_effective_workday_calculation.sql` เพิ่มโหมด/ขอบเขตและ Audit แบบ tenant-scoped; ยังไม่ apply และยังไม่ deploy เพราะต้องรออนุมัติ Migration. `test:effective-workday`, `test:reports-pay-period-export`, lint และ build ผ่านทั้งหมด. Status: `review`.

> EFFECTIVE-WORKDAY-001 Production completion (13/8/2569): ได้รับอนุมัติแล้วและตรวจ migration list ก่อนดำเนินการ พบค้างเฉพาะ `202608130001`; `db push` apply เฉพาะ `202608130001_effective_workday_calculation.sql` สำเร็จ และตรวจซ้ำพบ local/remote ตรงกัน. `test:effective-workday`, `test:reports-pay-period-export`, lint และ local/Vercel build ผ่าน. Vercel deployment `dpl_DqQs37d16qSGGn7CSD3Ak8NQRhWQ` READY และ alias `https://wisdomai-react.vercel.app`; Production smoke `/reports`, main bundle และ `Reports-CC7Xu3oA.js` HTTP 200 พร้อม marker `override_mode`, `half_morning`, `half_afternoon` และ `custom_period`. Status: `done` 100% / `deployed_smoke_passed`.

> EFFECTIVE-WORKDAY-001 regression repair (13/8/2569): เปิดแถวเดิมเพื่อตรวจซ้ำหลัง UAT พบ 3 สาเหตุร่วม ได้แก่ครึ่งวันที่ระบบคำนวณเองยังคืนค่าสาย/ออกก่อนจากกะเต็มวัน, การแก้เวลาอาจเหลือ `worked_minutes` เดิม, และโหมดอัตโนมัติเดาช่วงเช้าเมื่อเวลาเข้า–ออกคลุมทั้งเช้าและบ่าย. แก้กฎกลางให้ตรวจครึ่งวันเช้าเทียบช่วงเช้า, เข้าบ่ายเทียบช่วงบ่าย, กรณีคลุมสองช่วงเป็น `รอตรวจ` พร้อมเหตุผลเพื่อให้ Admin เลือกช่วง/ปรับผลจ่ายโดย Audit; เวลาเข้า–ออกจริงยังคงเป็นหลักฐานและผลระบบแยกจากผลจ่าย. คำนวณเวลาทำงานใหม่จากหลักฐานล่าสุดหักเวลาที่ไม่นับ และเพิ่ม regression test สำหรับเช้า บ่าย และกรณีกำกวม. `test:effective-workday`, lint, local build และ Vercel build ผ่าน. Production deployment `AMtP1y1J1XQTvEXTeEQhjvp1cbTh` alias `https://wisdomai-react.vercel.app`; smoke `/reports` HTTP 200 และ `Reports-Dk6wQ7Uo.js` HTTP 200 พร้อม marker ข้อความกรณีกำกวม, `half_afternoon` และการหัก `excluded_minutes`. Status: `done` 100% / `deployed_smoke_passed`.

> Function Test process correction (13/8/2569): เพิ่ม `docs/FUNCTION_TEST_ACCEPTANCE.md` เป็นเกณฑ์บังคับสำหรับ Attendance/ค่าแรง/Reports/Export และเพิ่มคำสั่งรวม `test:function:payroll-attendance`. การผ่าน lint/build/HTTP 200/bundle marker ไม่ถือว่าผ่าน business acceptance อีกต่อไป; ต้องตรวจ expected/actual จากเวลาเข้า–ออกจริง ครึ่งวันเช้า/บ่าย กรณีกำกวม grace period การคำนวณหลังแก้เวลา และความตรงกันของ Web/CSV/PDF พร้อม authenticated data verification หลัง Deploy. จาก UAT ล่าสุดตัวเลขบางแถวยังไม่ตรง expected จึงแก้สถานะ EFFECTIVE-WORKDAY-001 กลับเป็น `doing` จนกว่าจะผ่านเกณฑ์ใหม่นี้ครบ.

> EFFECTIVE-WORKDAY-001 Production regression fix (13/8/2569): พบสาเหตุที่ Production ยังแสดงสาย/ออกก่อนเทียบกะเต็มวันเมื่อ attendance session ไม่มี scheduled snapshot คือหน้า Reports ไม่ได้โหลดเวลาเริ่ม/จบกะ ช่วงพัก และ grace period จาก `work_policies`. แก้ให้โหลด Policy ที่มีผลเป็น fallback, แยกครึ่งวันเช้า 08:00–12:00 และครึ่งวันบ่าย 13:00–17:00, ใช้ grace period 5 นาที และคำนวณเวลาทำงานใหม่จากหลักฐานล่าสุดหัก `excluded_minutes`. Expected ที่ผ่านชุดทดสอบ: 07:37–12:07 = สาย 0/ออกก่อน 0/ออกเกิน 7 นาที; 14:00–18:00 = สาย 55/ออกก่อน 0/ออกเกิน 60 นาที; 08:43–17:01 ที่ผลจ่ายครึ่งวัน = รอตรวจ; 08:30–18:00 เต็มวัน = สาย 25/ออกก่อน 0/ออกเกิน 60 นาที. `test:function:payroll-attendance`, lint และ build ผ่าน; Vercel Production deployment `4fk51od9tNLjs15qAAgcrwASWfmC` สำเร็จและ alias `https://wisdomai-react.vercel.app`. การตรวจหน้า signed-in แบบอัตโนมัติถูก Browser URL Policy ปฏิเสธ และการตรวจ HTTP จาก sandbox เชื่อมต่อไม่ได้ จึงคงสถานะ `doing` ตามเกณฑ์ Function Test จนผู้ใช้ refresh และยืนยันค่าจริงบนหน้า Production; ไม่อ้างว่า UAT ผ่าน.

> PAYROLL Reports visual follow-up (12/8/2569): Employee daily detail rows now use distinct status colors for full work, half day, leave, holiday, stopped/no-attendance, incomplete attendance, and future dates. A full-period totals panel was added below the table for net time, late/early/outside-shift totals, payable wage days, base wage, OT, and accumulated net pay. Individual PDF output uses the same status colors and includes a final totals row. Verification passed: lint 0 errors (one pre-existing Reports hook warning), build, and `realtime-payroll-forecast.test.ts`. Production smoke loaded `Reports-Bsj_nqdr.js` and confirmed the net-time, payable-day, accumulated-net, color, and PDF-total markers.

> PAYROLL-WAGE-OVERRIDE-001 prepared (12/8/2569): เพิ่มการปรับผลคิดวันรายวันโดย Admin เป็น 0/0.5/1 วัน โดยไม่แก้เวลาเข้า–ออกหรือหลักฐานต้นฉบับ บังคับเหตุผล แสดงค่าระบบเดิมเทียบค่าที่ใช้จริง และเก็บ tenant, ผู้แก้, เวลา, ค่าเดิม/ค่าใหม่ใน audit แบบ append-only; การกดซ้ำอัปเดตรายการเดิมและเพิ่มประวัติใหม่ ไม่สร้างยอดซ้ำ. เตรียม Migration `202608120006_wage_day_override_audit.sql` และหน้า Reports แล้ว; `test:wage-day-override`, lint (0 errors, warning เดิม Reports 1 จุด) และ build ผ่าน. สถานะ `review` รออนุมัติ Migration และ Deploy Production.

> PAYROLL-WAGE-OVERRIDE-001 Production completion (12/8/2569): ได้รับอนุมัติ Migration/Deploy แล้ว ตรวจ migration list ก่อนทำพบค้างเฉพาะ `202608120006`; `supabase db push` นำขึ้น Production สำเร็จและตรวจซ้ำ local/remote ตรงกัน. `test:wage-day-override`, lint (0 errors, warning เดิม Reports 1 จุด), local build และ Vercel build ผ่าน. Vercel deployment `HNBhHP3MrH9t8aQ2SSrh9APfp58o` aliased ที่ `https://wisdomai-react.vercel.app`; smoke `/reports`, main bundle และ `Reports-DvHVYdhw.js` HTTP 200 พร้อม marker ปุ่มปรับผลคิดวัน, Audit และ RPC ครบ. สถานะ `done` 100% / `deployed_smoke_passed`.

> REPORT-TIME-CONFIG-001 prepared (12/8/2569): Added a company-scoped work-time display policy to the existing `workforce_rule_settings` contract. Defaults are days as the primary unit, two decimals, and hours/minutes as secondary detail; one day uses the existing company `full_day_minutes`. Reports and individual PDF read the shared preference, while OT, lateness, early leave, attendance, and payroll formulas remain unchanged. The setting UI is under Workforce Setup > time and wage rules. Migration `202608120002_work_time_display_config.sql` is prepared but not applied. Regression test, lint (0 errors; one pre-existing Reports hook warning), and build passed. Status: review pending explicit Migration and Production deploy approval.

> REPORT-TIME-CONFIG-001 Production completion (12/8/2569): Explicit approval was received. Worker heartbeat check found no running worker and the authoritative queue had no existing `REPORT-TIME-CONFIG-001` row, so no duplicate row was created and this status file remains the fallback evidence. Migration list confirmed `202608120002` was the only pending migration; it was applied without `--include-all`. Vercel deployment `BKpFR7JKYLcLEzEBdVP6EkerUEtj` completed and is aliased to `wisdomai-react.vercel.app`. Production smoke passed: new schema columns returned HTTP 200, `Reports-DqbBsUFF.js` reads the shared unit/decimal settings, and `WorkforceSetup-DJWF9XHc.js` contains the display-policy controls. Regression test, lint (0 errors; one pre-existing Reports hook warning), local build, Vercel build, migration, deploy, and smoke all passed. Status: done 100%.

> Workforce duplicate-profile visibility follow-up (12/8/2569): Workforce readiness now detects repeated normalized employee names in the loaded company list and displays a duplicate-count badge, employee code, email, and short Profile ID so same-name records can be distinguished. An incomplete duplicate is marked as a possible legacy/duplicate Profile and its action is labeled `ตรวจข้อมูลซ้ำ`; no Profile is merged, disabled, or deleted automatically. Regression test, lint (0 errors; one pre-existing Reports hook warning), local/Vercel build, deploy, and Production smoke passed. Deployment `HN8q6MgYqgqSsupVMro67yKBb8km` is aliased to Production; bundle `WorkforceSetup-DG05lwvA.js` contains all four duplicate-visibility markers.
> SYS-010 atomic specific-claim hardening (15/8/2569): เตรียม Migration `202608150003_atomic_specific_work_item_claim.sql` และปรับ `automation-worker` action `start_specific` ให้เรียก RPC เดียวซึ่ง lock งาน, ตรวจ `approved_for_execution`, persistent approval fingerprint, สร้าง `system_worker_runs` และเปลี่ยนงานเป็น `doing` ภายใน transaction เดียว ป้องกันงานค้าง `doing` โดยไม่มี run เมื่อ insert ล้มเหลว; จำกัด execute เฉพาะ `service_role`. `test:automation-claim`, lint และ build ผ่าน; Deno CLI ไม่มีใน environment จึงยังไม่ได้รัน `deno check`. ยังไม่ใช้ Migration หรือ Deploy Function ขึ้น Production และคงรออนุมัติการเปลี่ยน schema/security/deploy.

> SYS-004/SYS-010 Production completion (15/8/2569): ผู้ใช้อนุมัติ Migration `202608150001–202608150003` และ Deploy ตาม Approval Gate. ตรวจ remote ก่อนทำพบ `150001` และ `150002` อยู่ Production แล้ว เหลือเฉพาะ `150003`; `supabase db push` ใช้เฉพาะ `202608150003_atomic_specific_work_item_claim.sql` สำเร็จ และ migration history หลังทำยืนยัน local/remote ตรงกันทั้งสามรายการ. Deploy `automation-worker`, `health-monitor`, `line-webhook` สำเร็จ; Vercel Production deployment `dpl_BaMZMJovZAsAG8bhhpPz1c3shVcE` READY และ alias `https://wisdomai-react.vercel.app`. Smoke test: `/system-health` HTTP 200; anonymous `automation-worker`, `health-monitor` และ invalid LINE signature ตอบ 401; Production lazy bundle `SystemHealth-BhUZry_H.js` มีทะเบียนปัญหา, `system_error_events` และ `resolve_system_error_event`. Regression `system-error-intake`, `test:automation-claim`, `test:problem-register`, lint และ build ผ่าน. ไม่ claim งานจริงเพื่อทดสอบ transaction เพื่อหลีกเลี่ยงการเปลี่ยนคิว Production; การยืนยัน signed-in resolution/claim UAT ยังควรทำจาก Admin session.

> SYS-004 Error lifecycle reconciliation (15/8/2569): ผู้ใช้อนุมัติ Migration `202608150004` และ Deploy `health-monitor`/Web Production. เพิ่ม service-role RPC ปิด Health Error จากผล healthy ล่าสุดโดยไม่ผูกกับการมี incident row, ปิด Web Error หลัง quiet window 15 นาทีพร้อม Audit, backfill Historical Error, กรองทะเบียนตาม active company และรองรับ legacy `"[object Object]"`. Migration history ตรงกัน, `health-monitor` Deploy สำเร็จ และ Vercel deployment ล่าสุด `dpl_5N1LFKg6DAY2o1Piz3ZmRdPDLTgK` READY/aliased. Signed-in Admin UAT รอบ 12:08–12:09 ยืนยันบริษัท WisdomAI Construction ปกติ 9/9, Error เปิด 0, ทะเบียนค้าง/กำลังแก้/รอตรวจ/ติดปัญหาเป็น 0 และ Error ที่ปิดแล้ว 9 พร้อมเหตุผล Audit; Console ไม่พบ Error. SYS-004 ยังคงทำงานระดับ platform เพราะหลักฐาน worker ระบุ incident เปิดอีก 1 ใน scope อื่น ไม่ใช่บริษัทที่ UAT รอบนี้ตรวจ จึงไม่ปิดงานกลางแบบผิดข้อเท็จจริง.

> SYS-004 Auto Resolve Web Error (15/8/2569): ผู้ใช้อนุมัติ Deploy `health-monitor` ให้ปิดเหตุการณ์ Web ที่ยัง `open/monitoring` เฉพาะ tenant เดิม เมื่อผลตรวจ Client เป็น healthy และ `last_seen_at` ไม่มี Error ใหม่อย่างน้อย 15 นาที; fingerprint เดิมจะเปิดใหม่ได้เมื่อเกิดซ้ำ. ก่อน Deploy ตรวจ worker heartbeat และรอ local worker จบโดยไม่รันซ้อน จากนั้น claim SYS-004 แถวเดิมด้วย run `62eaf8f5-f2fc-4f79-8781-7de7e277d0bb`. `test:system-error-intake`, `test:health-status-report`, lint และ build ผ่าน; Deploy Supabase Function `health-monitor` Production สำเร็จ; anonymous smoke ตอบ 401 Unauthorized ตามสิทธิ์. คงสถานะ review 99% จน Admin กด “ตรวจทันที” หลัง Deploy และยืนยันจำนวน Error เปิดลดลงตามเงื่อนไข.

> SYS-004 ปิดงานตามคำสั่งผู้ใช้ (15/8/2569): ตรวจยืนยัน Migration `202608150004_error_lifecycle_reconciliation.sql` อยู่ Production แล้วและไม่มี worker ซ้อน ผู้ใช้สั่งปิด SYS-004 และจะตรวจสอบซ้ำในงาน/ห้องอื่น จึงปิดแถวเดิมเป็น `done` 100% ผ่าน run `5f3e61bf-8dcc-41d8-81f1-89e6585efdbd`. Production status คือ `deployed_user_closed_followup_verification_separate`; ไม่อ้างว่า authenticated UAT ผ่าน และเก็บข้อเท็จจริงว่าการยืนยันผล Auto Resolve หลัง Run now จะทำในงานติดตามแยกต่างหาก.

### WORKFORCE-SITE-POLICY-001 — ประวัติย้ายไซต์ Multi-site และ Policy Resolution (12/8/2569)
- สถานะ: `เสร็จแล้ว` 100% | Migration `202608120003` เปลี่ยนการมอบหมายไซต์เป็นรายการมีช่วงวันที่ รองรับไซต์หลัก/รอง ตารางเฉพาะรายการ และกลับไซต์เดิมโดยไม่ทับประวัติ
- การเลือกตาราง: รายการมอบหมาย → พนักงาน → ไซต์ และ `attendance-clock` บันทึก assignment/policy snapshot ตอนลงเวลาเพื่อรักษาผลย้อนหลัง
- หลักฐาน: regression tests, lint (0 errors; warning เดิม Reports 1 จุด), build ผ่าน; Function และ Vercel Production deploy สำเร็จ; `/employees`, `/workforce-setup`, `/time-tracking` HTTP 200; Function ไม่มีสิทธิ์ HTTP 401

> LINE-GROUP-APPROVAL-001 tenant-resolution repair (15/8/2569): Production UAT พบกลุ่มใหม่ `รายงานประจำวัน wisdom` ถูกผูก WisdomAI Construction และประมวลผลทันที เพราะ `resolveEventCompanyId()` fallback ไป tenant ของผู้ส่งเมื่อ Group ID ยังไม่มี mapping. แก้ `line-webhook` ให้ event ที่มี group/room ยอมรับ tenant จาก `line_groups` เท่านั้น และ return `null` ทันทีเมื่อไม่รู้จัก เพื่อเข้ากระบวนการ `register_unassigned_line_group`; direct message ยังใช้ sender/account fallback ตามเดิม. เพิ่ม regression ป้องกัน sender-based tenant inheritance; `test:line-group-assignment-approval`, `test:line-group-company`, `test:line-account-tenant`, lint และ build ผ่าน. Deploy `line-webhook` Production สำเร็จและ invalid-signature smoke ตอบ 401. คงสถานะ `review` 99% รอ UAT ด้วย Group ID ใหม่; ไม่ลบหรือย้าย mapping เดิมโดยไม่ได้รับอนุมัติ.

> LINE-GROUP-APPROVAL-001 web fallback follow-up (15/8/2569): UAT กลุ่ม `test line` ยืนยันด้วยภาพว่ามีข้อความ `Test 4` แต่ Telegram Admin ไม่ได้รับคำขอ และไม่มี pending request บนเว็บ. เพิ่มช่องทางสำรองใน Line Monitor > จัดการกลุ่ม LINE ให้ Platform Admin เห็น `line_group_assignment_requests` ที่รอผูก, เห็นสถานะ/ข้อผิดพลาด Telegram, เลือก option และเรียก `approve_line_group_assignment` ได้โดยตรง; ข้อมูลก่อนอนุมัติยังไม่เข้า tenant. Regression, lint และ build ผ่าน; Vercel deployment `2U8G7ECzXfN952nUFYvtnYtWwFu8` aliased Production. Signed-in smoke ยืนยันหน้าเดิมโหลดได้ แต่ยังไม่มี pending request ของ `test line` จึงสรุปว่า Message Event ของกลุ่มนี้ยังไม่ถึง `register_unassigned_line_group`; คง `review` 99% และไม่อ้างว่า UAT ผ่าน.

> LINE-GROUP-APPROVAL-001 webhook intake audit (15/8/2569): ผู้ใช้อนุมัติ Migration `202608150012`. เพิ่มตาราง audit ก่อนรู้ tenant โดยเก็บเฉพาะ hash/metadata/status ไม่เก็บ raw payload, signature, token หรือ secret; RLS อนุญาตอ่านเฉพาะ Platform Admin และ write ผ่าน service role. `line-webhook` บันทึก signature rejection, payload rejection, join/message/redelivery, tenant resolution, quarantine และผลสำเร็จ/ล้มเหลวแบบ deduplicate ด้วย fingerprint. LINE Monitor แสดงเส้นทางรับ Webhook ล่าสุดให้ Platform Admin. Functional regressions 4 ชุด, lint และ production build ผ่าน; Migration local/remote ตรงกันที่ `202608150012`, line-webhook Production ACTIVE version 57, Vercel deployment `8S66i6WfSuxK6Ty7SQg2yJXYeGXs` alias `wisdomai-react.vercel.app`. Smoke: `/line-monitor` HTTP 200 และ webhook ที่ไม่มีลายเซ็นตอบ 401. ยังต้องส่งข้อความจากกลุ่ม LINE ใหม่หนึ่งครั้งเพื่อยืนยัน valid signed event/quarantine แบบ end-to-end ก่อนปิดงาน 100%.

> LINE-GROUP-APPROVAL-001 RPC ambiguity fix (15/8/2569): UAT ข้อความ `ทดสอบ 22` จากกลุ่มใหม่ `Test Line 2` เข้า webhook audit เวลา 15:59:35 แต่ quarantine ล้มเหลวด้วย PostgreSQL `42702` เพราะ RETURNS TABLE variable `request_id` ชนกับคอลัมน์ใน `ON CONFLICT(request_id,company_id)`. ผู้ใช้อนุมัติ Migration `202608150013`; แก้ RPC ให้ target named constraint `line_group_assignment_options_request_id_company_id_key`, คง service-role boundary และสถานะงานเป็น review 99%. Regression 4 ชุด, lint, build และ migration dry-run ผ่าน; local/remote ตรงกันที่ `202608150013`, `line-webhook` Production ACTIVE version 58 และ invalid-signature smoke ตอบ 401. รอผู้ใช้ส่งข้อความกลุ่มใหม่อีกครั้งเพื่อยืนยัน quarantine และ pending assignment แบบ end-to-end ก่อน done.

> LINE-GROUP-APPROVAL-001 end-to-end quarantine UAT (15/8/2569): หลังแก้ RPC ระบบรับ signed message จาก `Test Line 2` (Group ID ลงท้าย `...d68c0`) เวลา 16:13:54, audit แสดง `awaiting_company_assignment`, สร้าง pending request/options สำเร็จ และหน้า Platform Admin แสดง `กลุ่มใหม่รอเลือกบริษัท (1)`. ข้อมูลยังไม่ถูก route เข้า tenant ตามข้อกำหนด. Telegram notification แสดง failed จึงใช้ web fallback ได้จริง. คง review 99% รอผู้ใช้เลือกบริษัทเจ้าของกลุ่มและอนุมัติการผูก; ห้ามเลือกแทนเพราะเป็นการตัดสินใจ tenant ownership.
> LINE-GROUP-APPROVAL-001 validation follow-up (15/8/2569): Re-ran `npm.cmd run lint`, `npm.cmd run build`, `test:line-webhook-intake`, `test:line-group-assignment-approval`, `test:line-group-company`, and `test:line-account-tenant`; all passed. Existing production evidence records migration `202608150012`, LINE webhook version 57, and the Vercel alias as deployed. Status remains `review` 99% because completion still requires a valid signed event from a genuinely new LINE group and confirmation that quarantine plus Admin notification/selection works end to end; no migration, permission/security, secret, or production data change was made in this follow-up.
> LINE-GROUP-APPROVAL-001 approval failure diagnosis (15/8/2569): Production UAT for `Test Line 2` reached the pending-company screen, but approving the selected company did not complete. Code inspection found a second PostgreSQL ambiguity in `approve_line_group_assignment`: its `RETURNS TABLE` output variable `line_group_id` conflicts with `ON CONFLICT(line_group_id)`. Migration `202608150014_line_assignment_approval_rpc_ambiguity_fix.sql` now uses the named unique constraint and fully qualified request/option aliases. The LINE Monitor error banner was made sticky so RPC failures remain visible at the current scroll position. Regression test `test:line-group-assignment-approval`, lint, and production build pass. Status remains `review` 99%; Migration 202608150014 and LINE Monitor Production deploy require explicit approval before final UAT assigns `Test Line 2` to WisdomAI Construction.
> LINE-GROUP-APPROVAL-001 Migration 202608150014 UAT (15/8/2569): Explicit approval received. Migration list showed only 202608150014 pending; it was applied to Production successfully. LINE Monitor deployed through Vercel deployment `4gSNstkcruTjXKQzDbTZ4yMphjQC` and aliased to `wisdomai-react.vercel.app`. Production smoke loaded the Platform Admin group manager and showed `Test Line 2`. Final approval UAT exposed a second schema mismatch: `column profile.platform_role does not exist`. The canonical database authorization is `profiles.role` through `is_platform_admin()`, while `platform_role` is only a client-side alias. Migration `202608150015_line_assignment_platform_admin_check_fix.sql` now uses `is_platform_admin()` for authenticated users and validates `profiles.role='admin'` for service-role approvals, preserving actor matching and tenant isolation. Status remains `review` 99%; Migration 202608150015 requires explicit approval before final assignment UAT.
> LINE-GROUP-APPROVAL-001 completed (15/8/2569): User explicitly approved Migration `202608150015`. Migration list confirmed it was the only pending migration and Production db push applied it successfully. Signed-in Platform Admin UAT approved `Test Line 2`; the pending-company card and approval button disappeared, and the active mapping now reads `Test Line 2 · บริษัทปัจจุบัน: WisdomAI Construction` with the company selector fixed to WisdomAI Construction. This confirms the full signed LINE event → pre-tenant quarantine → Admin company selection → tenant mapping flow. Functional approval regression, lint, local build, Vercel Production build, migrations 202608150014/202608150015, authenticated smoke, and tenant ownership UAT passed. Status: done 100%; Production status: `deployed_assignment_uat_passed`.
> LINE-GROUP-APPROVAL-001 authoritative closure (15/8/2569): User explicitly approved Migration `202608150016`. The guarded reconciliation updated only the existing platform-scoped row, cleared worker lease fields, and relied on the existing work-item audit trigger. Functional regression, lint, build, and migration dry-run passed before Production apply. Local/remote migration history matches through `202608150016`; authenticated Work Command Center smoke shows the existing row under `เสร็จแล้ว` with `completed` and `100%` at 15/8/2569 17:44:07. No duplicate work item was created.
> WORKFORCE-SITE-LIFECYCLE-001 authoritative closure (15/8/2569): The previously approved Migration `202608120005` was confirmed on Production. Site-assignment lifecycle regression, lint, and build passed; authenticated Workforce Setup smoke for WisdomAI Construction showed active assignment history and per-row management controls. User explicitly approved Migration `202608150017`; its guarded reconciliation updated only the existing platform-scoped work item and did not mutate tenant workforce data. Local/remote history matches through `202608150017`; Work Command Center shows `completed`, `เสร็จแล้ว`, and `100%` at 15/8/2569 17:54:13. No duplicate work item was created.
> SYS-004 verification refresh (15/8/2569): Updated evidence for the existing `review` 90% work item without creating a duplicate. `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, lint, and build all passed (exit 0; bundle `SystemHealth-CF-ktAz_.js`). This revalidates tenant-safe intake, fingerprint dedupe, and LINE screenshot evidence correlation. No migration, deployment, schema/RLS/permission change, secret access, or Production mutation was performed; Production status remains unchanged.
> SYS-004 expected Auth outcome follow-up (15/8/2569): Signed-in System Health verification found two open `/auth/v1/token` HTTP 400 events with different frontend fingerprints, plus their derived client-warning event. Login and the remaining nine health paths were available, so these are expected authentication rejections rather than outages. Prepared Migration `202608150018_expected_auth_error_reconciliation.sql` to dismiss only the matching open Auth outcomes and their derived tenant warnings, then reconcile the existing SYS-004 row without creating a duplicate. App telemetry now excludes Auth token HTTP 400/401 from central outage intake. `test:system-error-intake`, `test:problem-register`, lint, and build pass. Migration and Web Production deploy are not yet applied and require explicit approval; SYS-004 remains open until Production migration, deploy, health check, and authenticated smoke all pass.
> SYS-004 Migration 202608150018 Production verification (15/8/2569): Explicit approval received. Dry-run confirmed `202608150018_expected_auth_error_reconciliation.sql` was the only pending migration; Production apply succeeded and local/remote history now matches through `202608150018`. Vercel deployment `5pnWERw55gYBzxDYKP1LTraLiHHe` completed and was aliased to `https://wisdomai-react.vercel.app`. Auth 400 incidents `ERR-A26CF243` and `ERR-C95FF244` now show resolved with the expected-auth audit reason. Authenticated smoke also found remaining non-Auth incidents: image optimizer HTTP 546, communication feed HTTP 500, a native `prompt()` unsupported client event, and a derived client warning. Therefore the authoritative SYS-004 row truthfully remains `doing` 90%; it was not force-closed and no duplicate work item was created.
> SYS-004 safe verification refresh (15/8/2569): Revalidated the existing `review` 90% work item without creating another task. `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, lint, and build all passed (exit 0; bundle `SystemHealth-BCho1_pO.js`). This confirms the source coverage for tenant-safe central intake, fingerprint dedupe, and correlation of LINE screenshot evidence. Migration `202608150018` and the Web Production deploy remain unapplied because this verification did not authorize schema or deployment changes; Production status is unchanged and the remaining gate is approved migration/deploy plus authenticated smoke.
> SYS-004 source evidence refresh (15/8/2569): Updated the existing `review` 90% item only; no duplicate task was created. Focused regressions `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, and `test:line-webhook-intake` passed, as did lint and build (exit 0; bundle `SystemHealth-DXRtA0sx.js`). The build check found and safely corrected the System Health audit-reason TextField to use the current MUI `slotProps.htmlInput` API while preserving its 1,000-character limit. This refresh performed no migration, deployment, schema/RLS/permission change, secret operation, or Production mutation; Production remains as recorded by the prior `202608150018` deployment verification, with non-Auth incidents still requiring operational resolution and authenticated UAT.

> SYS-004 urgent runtime repair prepared (15/8/2569): แก้สาเหตุที่ยืนยันจาก Production 3 จุดในแถวเดิมโดยไม่สร้างงานซ้ำ: (1) เอา `window.prompt()` ออกจาก System Health และใช้ Audit Dialog ที่บังคับเหตุผล (2) เปลี่ยน Communication Feed ไปใช้ `get_communication_event_feed` ซึ่งตรวจสิทธิ์ company manager/platform admin ก่อนอ่านข้อมูลบริษัท และ (3) เอา ImageMagick WASM ออกจาก `image-storage-optimizer` เพื่อหลีกเลี่ยง Edge HTTP 546 โดยใช้ Storage image transformation ทีละไฟล์. เตรียม Migration `202608150019_system_health_runtime_repairs.sql`; `test:system-error-intake`, `test:communication-event-feed`, lint และ build ผ่านทั้งหมด. ยังไม่ Apply Migration หรือ Deploy Production; รออนุมัติ Migration และ Deploy Web/`image-storage-optimizer` ก่อนทำ Production smoke.
> SYS-004 source verification refresh (16/8/2569 03:51 Asia/Bangkok): Updated evidence on the existing `review` 90% work item only; no duplicate task was created. All 9 focused regressions passed: `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, and `test:line-webhook-intake`. Build passed (exit 0; bundle `SystemHealth-Cx_PaavK.js`). Full lint is blocked only by 2 reports of the pre-existing unrelated `no-constant-binary-expression` finding at `src/pages/AccountingDocuments/index.tsx:752`; that user-owned feature work was preserved. No migration, deployment, schema/RLS/permission change, secret operation, data deletion, or Production mutation was performed. Production remains `deployed_runtime_uat_passed`; source verification remains at review 90% with fingerprint `lint:no-constant-binary-expression:src/pages/AccountingDocuments/index.tsx:752`.

> SYS-004 runtime repair verification (15/8/2569): Updated evidence for the existing 90% work item without creating a duplicate. Verified the three prepared repairs: System Health uses a required-reason Audit Dialog instead of `window.prompt()`, Communication Feed reads through the company-guarded `get_communication_event_feed` RPC, and `image-storage-optimizer` uses Storage image transformations without ImageMagick WASM. `test:system-error-intake`, `test:communication-event-feed`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, lint, and build all passed (exit 0; bundle `SystemHealth-Uodj4Prd.js`). No migration, deployment, schema/RLS/permission change, secret operation, data deletion, or Production mutation was performed in this verification. Existing Production deployment evidence is retained below; authenticated optimizer UAT remains pending.

> SYS-004 runtime repair Production deployment (15/8/2569): Explicit approval received for Migration `202608150019` and the Web/`image-storage-optimizer` deployment. Production `db push` applied `202608150019_system_health_runtime_repairs.sql`; local/remote migration history matches through `202608150019`. `image-storage-optimizer` is ACTIVE version 4. Vercel deployment `dpl_AuuTRSenMNXmLGCYYyTtyH1VWFR2` is READY and aliased to `https://wisdomai-react.vercel.app`. Smoke evidence: `/system-health` HTTP 200; the unauthenticated optimizer call returns expected HTTP 401; signed-in System Health loads 29 communication-log rows without a partial-load error; the error workflow opens a required-reason Audit Dialog with a 1,000-character limit instead of a native prompt. Pre-deploy `test:system-error-intake`, `test:communication-event-feed`, lint, and build passed. The existing SYS-004 item remains at 95% until an authenticated optimizer job processes a real pending image without HTTP 546; that UAT mutates stored image objects and was intentionally not run as a read-only smoke test. No duplicate work item was created.

> SYS-004 authenticated runtime UAT (15/8/2569): User approved the real-image UAT. System Health reported 1,187 images processed, 0 pending, 0 failed, and 77.9 MB saved; the optimizer action displayed `ดำเนินการครบแล้ว`, and browser diagnostics contained no new HTTP 546 or optimizer failure. Communication Feed still loaded successfully, and the Audit Dialog remained operational. Admin resolved the four verified historical incidents (`ERR-F5FA5368`, `ERR-298EF0FA`, `ERR-57DA638E`, `ERR-0ED643D1`) with distinct audit reasons. A subsequent `ตรวจทันที` run reported 0 open central errors and the problem register settled at 0 pending / 0 in progress / 0 verification / 20 resolved. Prepared guarded Migration `202608150020_system_health_runtime_repairs_completion.sql` to close only the existing platform-scoped SYS-004 row; it has not been applied and requires explicit approval.

> SYS-004 authoritative completion (15/8/2569): User explicitly approved Migration `202608150020`. Preflight found no active worker run and found that the Automation Worker had already completed the existing SYS-004 row at 100%; the reconciliation migration was made idempotent so it accepts either the expected `doing` 90% state or an already completed 100% state, while still targeting only the platform-scoped SYS-004 row. `test:work-item-reconciliation` passed. Production apply succeeded, and migration history confirms local/remote `202608150020`. The authoritative row now reports `done`, 100%, `completed`, `deployed_runtime_uat_passed`, with worker lease fields cleared. Status: done 100%; no duplicate work item was created.

> SYS-004 safe source revalidation (15/8/2569): Updated evidence for the existing 90% work item only; no duplicate task was created. Reconfirmed the required-reason Audit Dialog, company-guarded `get_communication_event_feed` RPC, and Storage image-transformation optimizer without ImageMagick WASM. `test:system-error-intake`, `test:communication-event-feed`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, lint, and build all passed (exit 0; bundle `SystemHealth-Uodj4Prd.js`). No migration, deployment, schema/RLS/permission change, secret operation, data deletion, or authenticated optimizer mutation was performed; Production status and the pending real-image HTTP 546 UAT gate remain unchanged.

> SYS-004 completion evidence refresh (15/8/2569): Updated the evidence trail for the existing SYS-004 item only; no duplicate task was created. Source revalidation passed `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, lint, and build (all exit 0; bundle `SystemHealth-Uodj4Prd.js`). This reconfirms central tenant-safe intake, fingerprint deduplication, audited resolution, tenant-safe communication feed, LINE evidence handling, and the Storage image-transformation optimizer. The authoritative evidence immediately above remains controlling: SYS-004 is `done` 100% with `deployed_runtime_uat_passed`; the preceding safe-source note's pending-UAT statement was stale. This refresh ran no migration or deployment, changed no schema/RLS/permission or secrets, and performed no Production mutation or data deletion.

> SYS-004 requested 90% source evidence refresh (15/8/2569): Updated the existing SYS-004 evidence only and created no duplicate task. Revalidation passed `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, `npm.cmd run lint`, and `npm.cmd run build` (all exit 0; bundle `SystemHealth-Uodj4Prd.js`). This confirms the requested completed scope: central tenant-safe error intake, fingerprint deduplication, audited resolution, tenant-safe communication feed, LINE evidence handling, and the Storage image-transformation optimizer runtime repair. The supplied tracking progress remains `review` 90%; existing Production evidence/status is unchanged. No migration, deployment, schema/RLS/permission or secret change, data deletion, or Production mutation was performed.

> SYS-004 source evidence revalidation (15/8/2569): Refreshed only the existing SYS-004 row and evidence trail; no duplicate task was created. `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, lint, and build all passed (exit 0; bundle `SystemHealth-BYqArq0W.js`). The checks cover central tenant-safe intake, fingerprint deduplication, audited resolution, tenant-safe communication feed, LINE evidence handling, and the Storage image-transformation optimizer runtime repair. Tracking remains `review` 90% as requested; authoritative Production evidence remains `deployed_runtime_uat_passed`. No migration, deployment, schema/RLS/permission or secret change, data deletion, or Production mutation was performed.

> SYS-004 evidence refresh (15/8/2569): Updated the existing work-item evidence only; no duplicate task was created. `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, `npm.cmd run lint`, and `npm.cmd run build` all passed (exit 0; bundle `SystemHealth-C9754qZ1.js`). This reconfirms the completed central error intake, fingerprint deduplication, audited resolution, tenant-safe communication feed, and image optimizer runtime repairs. Tracking remains `review` 90% per the supplied work item; Production remains `deployed_runtime_uat_passed`. No migration, deployment, schema/RLS/permission or secret change, data deletion, or Production mutation was performed.

> SYS-004 evidence refresh (16/8/2569): Updated only the existing SYS-004 evidence trail; no duplicate task was created and no source repair was required. `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, `npm.cmd run lint`, and `npm.cmd run build` all passed (exit 0; bundle `SystemHealth-DUf-Zlm0.js`). This revalidates central tenant-safe error intake, fingerprint deduplication, audited resolution, the tenant-safe communication feed, LINE evidence handling, and image optimizer runtime repairs. Tracking remains `review` 90% as supplied; the existing Production evidence remains `deployed_runtime_uat_passed`. No migration, deployment, schema/RLS/permission or secret change, data deletion, or Production mutation was performed.
> SYS-004 evidence refresh (16/8/2569, current verification): Re-ran and refreshed only the existing SYS-004 evidence trail; no duplicate task was created and no source repair was required. `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, `npm.cmd run lint`, and `npm.cmd run build` all passed in the current work-item run (exit 0; bundle `SystemHealth-C1xNGBRJ.js`). This revalidates central tenant-safe error intake, fingerprint deduplication, audited resolution, the tenant-safe communication feed, LINE evidence handling, and image optimizer runtime repairs. Tracking remains `review` 90% as supplied; the existing Production evidence remains `deployed_runtime_uat_passed`. No migration, deployment, schema/RLS/permission or secret change, data deletion, or Production mutation was performed.

> SYS-004 evidence refresh (16/8/2569, lint-blocked verification): Updated only the existing SYS-004 evidence trail; no duplicate task or SYS-004 source change was created. All nine focused regressions passed: `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, and `test:line-webhook-intake`. `npm.cmd run build` also passed (exit 0; bundle `SystemHealth-BDdohuro.js`). Repository-wide lint is blocked by unrelated in-progress user work at `src/pages/AccountingDocuments/index.tsx:700` (`no-constant-binary-expression`, fingerprint `LINT-ACCOUNTINGDOCUMENTS-L700-NO-CONSTANT-BINARY-EXPRESSION`); that large existing edit was preserved. Tracking therefore remains `review` 90%, while Production remains unchanged at `deployed_runtime_uat_passed`. No migration, deployment, schema/RLS/permission or secret change, data deletion, or Production mutation was performed.

# PLATFORM-CONTROL-CENTER-001 — prepared for review (not applied)

- Prepared Migration `202608150021_platform_control_center.sql`; Production has not been applied or deployed.
- Added Platform Admin-only route `/platform-control-center`, explicit `platform_role` guard, graphical KPI/health/trend/error/work/tenant/audit dashboard, and explicit company confirmation flow.
- The authoritative `public.system_work_items` row is included inside the prepared migration because a platform-scoped row cannot safely be created through the company-scoped Web RPC before this migration is approved.
- Gate: run functional test, lint and build locally; then request separate approval before Migration/Production deploy.

> PLATFORM-CONTROL-CENTER-001 Production completion (15/8/2569): User explicitly approved Production apply and deployment. Preflight found no overlapping worker run and the existing authoritative work item was claimed without creating a duplicate. Production migration history is aligned through `202608150022`; `202608150021_platform_control_center.sql` was already present remotely and `supabase db push` confirmed the database was up to date. Vercel Production deployment `52FfZRniRVqeuLpCqcpHexafhoaj` completed and is aliased to `https://wisdomai-react.vercel.app`. Functional contract, lint, build, and authenticated smoke passed. The Platform Admin-only `/platform-control-center` loaded aggregate KPIs, Service Health Map, 24-run trend, central Error/work panels, Company Tree, and Audit Timeline without a console error and without auto-selecting a tenant. Status: done 100%.

> PLATFORM-CONTROL-CENTER-001 completion gap repair prepared (16/8/2569): The existing Production completion claim was audited against the agreed scope and found incomplete. Without creating a duplicate work item, the source now moves Platform Mode into the company selector for Platform Admin only, adds real 24-hour/7-day/30-day windows, reconciles health to one card per module, replaces placeholder drill-down with source rows, and adds Error, LINE/Telegram, Workforce, tenant tree, work-item and audit views. Migration `202608160012_platform_control_center_completion.sql` is prepared with the Platform Admin DB guard and updates only the existing authoritative row when applied. Focused functional contract, focused lint, and Production build pass. Repository-wide lint remains blocked by two unrelated pre-existing `no-constant-binary-expression` findings in `src/pages/AccountingDocuments/index.tsx:829`, which was preserved. Supabase CLI dry-run could not be completed in the sandbox because fetching the CLI package was denied; no Migration or Production deploy was performed. Gate: explicit approval for Migration `202608160012` and Web Production deployment, followed by authenticated smoke before the row may return to `done`.

> PLATFORM-CONTROL-CENTER-001 authoritative completion (16/8/2569): User explicitly approved Migration `202608160012` and Web Production deployment. Preflight waited for the active SYS-004 worker to finish before proceeding and did not create a duplicate work item. Production was already aligned through `202608160012`; `supabase db push` reported up-to-date and migration history confirmed local/remote alignment. Vercel deployment `3kApahybACzTGypzXVM6Pwzb5izJ` built successfully and was aliased to `https://wisdomai-react.vercel.app`. Authenticated Platform Admin smoke passed aggregate KPIs, 24-hour data, one reconciled health card per module, Error fingerprint/system/user evidence, two-company tenant tree, tabs and zero console errors. The existing authoritative row was atomically claimed and finished as `done` 100% with `deployed_authenticated_smoke_passed`. Focused contract/lint and local/Vercel builds passed; repository-wide lint remains blocked only by the unrelated existing `AccountingDocuments/index.tsx:829` finding, which was preserved.

> SYS-004 evidence refresh (16/8/2569, current safe verification): Updated the existing SYS-004 evidence only; no duplicate task or source repair was created. All nine focused regressions passed: `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, and `test:line-webhook-intake`. `npm.cmd run build` passed (exit 0; bundle `SystemHealth-CfPY8Z1n.js`). Repository-wide lint remains blocked by unrelated in-progress user work in `src/pages/AccountingDocuments/index.tsx`: unused `setBulkItemType` at line 118 and two reports of `no-constant-binary-expression` at line 829 (fingerprint `lint:accounting-documents:unused-setBulkItemType@118+constant-binary-expression@829`), so that file was preserved. Tracking remains `review` 90%, and Production remains `deployed_runtime_uat_passed`. No migration, deployment, schema/RLS/permission or secret change, data deletion, or Production mutation was performed.

> SYS-004 evidence refresh (16/8/2569, latest safe verification): Updated only the existing SYS-004 evidence trail; no duplicate task or SYS-004 source repair was created. All nine focused regressions passed: `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, and `test:line-webhook-intake`. `npm.cmd run build` passed (exit 0; bundle `SystemHealth-BTiL885j.js`). Repository-wide `npm.cmd run lint` remains blocked by two unrelated `no-constant-binary-expression` reports in the user's in-progress `src/pages/AccountingDocuments/index.tsx:855` edit (fingerprint `lint:accounting-documents:no-constant-binary-expression@855`), which was preserved. Tracking remains `review` 90%, and Production remains `deployed_runtime_uat_passed`. No migration, deployment, schema/RLS/permission or secret change, data deletion, or Production mutation was performed.

> SYS-004 evidence refresh (16/8/2569, current work item run): Updated only the existing SYS-004 evidence trail; no duplicate task or SYS-004 source repair was created. All nine focused regressions passed: `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, and `test:line-webhook-intake`. `npm.cmd run build` passed (exit 0; bundle `SystemHealth-DaQhIykS.js`). Repository-wide `npm.cmd run lint` is blocked by unrelated in-progress user work in `src/pages/AccountingDocuments/index.tsx`: `react-hooks/set-state-in-effect` at line 416 and two `no-constant-binary-expression` reports at line 913 (fingerprint `lint:accountingdocuments:set-state-in-effect@416|no-constant-binary-expression@913`); that file was preserved. Tracking remains `review` 90%, and Production remains `deployed_runtime_uat_passed`. No migration, deployment, schema/RLS/permission or secret change, data deletion, or Production mutation was performed.

> SYS-004 verification confirmation (16/8/2569): Re-ran the same nine focused regressions and Production build against the current working tree; all passed, with `SystemHealth-DaQhIykS.js` produced again. Repository-wide lint reproduced only the preserved unrelated Accounting Documents findings under fingerprint `lint:accountingdocuments:set-state-in-effect@416|no-constant-binary-expression@913`. Existing work-item status and Production state remain `review` 90% / `deployed_runtime_uat_passed`; no duplicate work item or Production mutation was made.

> SYS-004 current evidence refresh (16/8/2569): Updated the existing work-item evidence only. The nine focused regressions (`test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, and `test:line-webhook-intake`) passed, and `npm.cmd run build` passed with bundle `SystemHealth-DaQhIykS.js`. Repository-wide `npm.cmd run lint` remains blocked only by unrelated user-owned `src/pages/AccountingDocuments/index.tsx` findings at line 416 (`react-hooks/set-state-in-effect`) and line 913 (`no-constant-binary-expression`, reported twice), fingerprint `lint:accountingdocuments:set-state-in-effect@416|no-constant-binary-expression@913`; that file was preserved. Tracking remains `review` 90%, Production remains `deployed_runtime_uat_passed`, and no migration, deployment, schema/RLS/permission or secret change, data deletion, or Production mutation was performed.

> SYS-004 current-turn evidence refresh (22/8/2569): Updated only the existing SYS-004 evidence trail; no duplicate task was created. Re-ran the same 13 focused regressions plus repository lint/build in this turn: `test:system-error-intake`, `test:error-fingerprint`, `test:work-item-reconciliation`, `test:problem-register`, `test:communication-event-feed`, `test:health-monitor-tenant`, `test:line-account-tenant`, `test:workforce-readiness-health`, `test:line-webhook-intake`, `test:system-performance-standard`, `test:auth-routing`, `test:chat-launcher-attachment`, and `test:line-attendance-command`; `npm.cmd run lint` and `npm.cmd run build` both passed, with build output `SystemHealth-BIPNkk3W.js`. No migration, deployment, schema/RLS/permission change, secret access, data deletion, or Production mutation was performed.
